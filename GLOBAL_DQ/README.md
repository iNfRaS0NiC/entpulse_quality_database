# GLOBAL DQ Template Registry

## Purpose

A GLOBAL DQ template is one approved data-quality check whose logic holds for every
applicable sport, stored once and executed for a selected sport by substituting its
declared parameters. It carries the full `POWERBI.md` contract — identity header,
findings-plus-coverage, audited-object granularity, commented scope filter — and differs
from a sport check only in that its sport-specific values are parameters instead of
literals.

This exists because the alternative does not scale. A hand-copied sport file is ~60 lines
per check; at 110 sports one logic fix would have to be applied 110 times, with nothing
detecting the copies that drifted. A template is fixed once and every sport that
instantiates it follows.

`GLOBAL_QUERIES/` answers *what is stored*. `GLOBAL_DQ/` asserts *what is wrong*. The
boundary between discovery and DQ is unchanged and owned by `POWERBI.md`.

## What is still not automatic

A template is not permission to check a sport. The authorization gate in `POWERBI.md`
applies unchanged: DQ work starts only after the user names a sport and a category, and a
check is instantiated for that sport only after the user approves it.

What changes is what approval produces. Instead of a new ~60-line statement, it produces:

1. one row in `POWERBI_REGISTRY.md` whose `Family` column names the template;
2. the parameter values for that sport in `SPORTS/params.json`, if not already recorded.

No per-sport copy of the SQL is created. A check that genuinely cannot be expressed
through declared parameters stays sport-authored, with `Family` = `—` and its statement in
`POWERBI_QUERIES/<SportSlug>.sql`.

## Parameter contract

Mandatory parameters use the same uppercase double-brace tokens as `GLOBAL_QUERIES/`, and
`WORKFLOW.md` owns the general rule. Two kinds occur:

| Kind | Substitution | Example |
|---|---|---|
| Scalar | A numeric or quoted value in a comparison | `{{SPORT_ID}}`, `{{STATISTIC_TYPE_ID}}` |
| Physical name fragment | Text inside a table or column name | `statistic_participants{{SHARD_ID}}` |
| List | A quoted, comma-separated list inside `IN (...)` | `{{EVENT_PARTICIPANT_TYPE_LIST}}` |

A list parameter's value already contains its quotes: `'athlete'` or `'athlete', 'team'`.
It is a SQL fragment, so it is only ever filled from `SPORTS/params.json`, never from
user-supplied text.

| Parameter | Meaning |
|---|---|
| `{{SPORT_ID}}` | Confirmed numeric `sport.id` |
| `{{EVENT_PARTICIPANT_TYPE_LIST}}` | Participant types the sport uses in `event_participants` |
| `{{REGISTRY_PARTICIPANT_TYPE_LIST}}` | Participant types present in the sport's `object_participants` registry |
| `{{STATISTIC_TYPE_ID}}` | Confirmed `statistic.statistic_typeFK` |
| `{{SHARD_ID}}` | Confirmed physical statistic participant/data shard number |
| `{{DATA_RANK_TYPE_ID}}` | `statistic_data_type.id` of the Rank data field for that statistic type |
| `{{CONFIG_START_DATE_TYPE_ID}}` | `statistic_data_type.id` of the Start date config field |
| `{{CONFIG_END_DATE_TYPE_ID}}` | `statistic_data_type.id` of the End date config field |
| `{{CONFIG_EVENT_ID_TYPE_ID}}` | `statistic_data_type.id` of the Event id config field |
| `{{DATA_COMMENT_TYPE_ID}}` | `statistic_data_type.id` of the Comment data field |
| `{{DATA_MEDAL_TYPE_ID}}` | `statistic_data_type.id` of the Medal data field |
| `{{DATA_TIME_TYPE_ID}}` | `statistic_data_type.id` of the current Time data field |
| `{{DATA_TIME_DIFFERENCE_TYPE_ID}}` | `statistic_data_type.id` of the Time Difference data field |
| `{{DATA_DEPRECATED_DURATION_TYPE_ID}}` | `statistic_data_type.id` of the superseded Duration data field |
| `{{RESULT_RANK_TYPE_ID}}` | `result_type.id` of the sport's Rank event result |
| `{{RESULT_COMMENT_TYPE_ID}}` | `result_type.id` of the sport's Comment event result |
| `{{RESULT_MEDAL_TYPE_ID}}` | `result_type.id` of the sport's Medal event result |
| `{{RESULT_DURATION_TYPE_ID}}` | `result_type.id` of the sport's leader/gap Duration event result |
| `{{CONFIG_GENDER_TYPE_ID}}` | `statistic_data_type.id` of the Gender config field |
| `{{FINAL_ROUND_TYPE_LIST}}` | Comma-separated `round_type.id` values the sport treats as its decisive Final round |
| `{{DUPLICATE_KEY_INCLUDES_EVENT_NAME}}` | `1` where the sport's event name carries information beyond which competitors take part, so two events sharing a slot and a participant set are told apart by it; `0` where the name is built from the participants themselves and therefore adds nothing but an arbitrary ordering. A heats-based sport records 1, because Moto 1 and Moto 2 are distinguished by name alone. A sport that instantiates `GLOBAL-DQ-096` records 0 by definition: that check asserts the name is made of the participants |
| `{{ELIMINATION_ROUND_NAME_LIST}}` | A quoted, comma-separated list of lower-case `round_type.name` values naming a round in which some entries go out and the rest go on, so the round is a knockout by definition |
| `{{GROUP_ROUND_NAME_LIST}}` | A quoted, comma-separated list of lower-case `round_type.name` values naming a round nobody is eliminated in, so the round is not a knockout. A name the sport cannot classify from its name alone - a placement or qualification round whose answer depends on the format - belongs in neither list and is left unjudged |
| `{{DUPLICATE_KEY_USES_CALENDAR_DAY}}` | `1` where one meeting between the same competitors happens at most once a day, so two of them on one date are a duplicate whatever their kick-off; `0` where the sport re-runs the same field several times in a day - motos, rounds of one heat series - and only the exact moment separates them. A sport recording 1 catches the re-import that arrived with a shifted kick-off; a sport recording 0 would report its whole heat structure |
| `{{BRONZE_ROUND_TYPE_LIST}}` | Comma-separated `round_type.id` values of the match that decides bronze, where the sport decides it outside the Final. Together with `FINAL_ROUND_TYPE_LIST` it says which medal each round awards, which a single medal-round list cannot: the Final decides gold and silver, the bronze match decides bronze. A sport awarding every medal in one event records a value that never occurs, such as `0` |
| `{{MEDAL_ROUND_TYPE_LIST}}` | Comma-separated `round_type.id` values on which the sport awards a medal at all. It is a superset of `FINAL_ROUND_TYPE_LIST` wherever a medal is decided outside the Final: a head-to-head sport settles bronze in its own match, so that round belongs here and the Final alone does not describe where medals occur. A sport awarding every medal in one event records the same values in both |
| `{{RANK_MAX_PLAUSIBLE}}` | Highest rank value the sport can legitimately award in one event |
| `{{NOT_STARTED_DESC_LIST}}` | Comma-separated `status_descFK` values under `status_type='notstarted'` that the sport expects to carry no results |
| `{{TIMED_DISCIPLINE_LIST}}` | Comma-separated `discipline.id` values in which the sport records a measured time; judged disciplines are excluded |
| `{{RESULT_FULL_TIME_TYPE_ID}}` | `result_type.id` of the sport's absolute full-time event result |
| `{{STALE_NOT_STARTED_DAYS}}` | Days after its start date beyond which a still not-started event is treated as stale. Uniform across every sport at `2` by decision of 2026-08-06: an event still marked not started two days after its own date is not a calendar published far ahead, it is a status nobody maintained |
| `{{PLACEHOLDER_COUNTRY_LIST}}` | A quoted, comma-separated list of `country.name` values the sport treats as a placeholder rather than a real country |
| `{{DATA_TEAM_TYPE_ID}}` | `statistic_data_type.id` of the Team data field, which holds the `participant.id` of the team an athlete competes for inside the statistic |
| `{{ROUND_TYPE_LIST}}` | Comma-separated `round_type.id` values the sport is confirmed to contest |
| `{{SCOPE_TYPE_ID}}` | One confirmed `scope_type.id` used by templates whose semantic rule applies to a single container type |
| `{{SCOPE_TYPE_LIST}}` | Comma-separated active `scope_type.id` values used by a relational check that must audit every confirmed scope container type |
| `{{NUMERIC_RESULT_TYPE_LIST}}` | Comma-separated `result_type.id` values that carry a measured quantity rather than text or a time |
| `{{NUMERIC_DATA_TYPE_LIST}}` | Comma-separated `statistic_data_type.id` values that carry a measured quantity rather than text or a time |
| `{{RESULT_COMMENT_VALUE_LIST}}` | A quoted, comma-separated list of the Comment result status codes the sport is confirmed to use |
| `{{DATA_COMMENT_VALUE_LIST}}` | A quoted, comma-separated list of the Comment data status codes the sport is confirmed to use |
| `{{RESULT_SCORE_TYPE_ID}}` | `result_type.id` of the single scalar score the sport decides its within-event ranking from. It is the quantity a shared place has to agree on, so a sport whose place is settled by a time, by a head-to-head outcome or by nothing stored has no value to record here |
| `{{RESULT_TIE_VALUE_TYPE_LIST}}` | Comma-separated `result_type.id` list of the fields a shared place has to agree on for the tie to be the result rather than a defect — a score for a judged sport, a full time or a duration for a timed one. A list rather than a single field because a sport may rank on more than one and store them unevenly; agreement on any one of them accounts for the tie. Record every field the sport actually settles a place from, and nothing it merely stores beside the place |
| `{{SERIES_MIN_YEARS}}` | Fewest editions a template must hold before its rhythm is trusted and a break in it is reported; three is the fewest that yields two intervals |
| `{{SERIES_SKIP_YEARS}}` | Comma-separated calendar years in which the sport did not run, so they are neither editions nor breaks; a sport with nothing to skip records a year that never occurs, such as `0` |

Every value is a confirmed structural fact recorded in the sport's file before it reaches
`SPORTS/params.json`. A parameter that is not yet confirmed for a sport means the template
cannot be instantiated for it yet — not that a plausible value may be guessed.

## Optional registry branch

A template that reaches people through the sport registry **as well as** through the
participation paths marks the registry branch optional, by bracketing it exactly:

```sql
        -- REGISTRY BRANCH BEGIN
        UNION ALL

        SELECT op.participantFK, 0, 0, 0
        FROM object_participants op
        WHERE op.object = 'sport'
          AND op.objectFK = {{SPORT_ID}}
          AND op.del = 'no'
        -- REGISTRY BRANCH END
```

`TOOLS/Run-Query.ps1 -WithoutRegistryBranch` removes every marked block, and the run is
refused where a template marks none.

The marker exists because the registry has no template relation and so survives every scope
narrowing. Where a sport's client scope is narrower than the sport — `SPORTS/Soccer.md` is the
confirmed case — a narrowed run of an unmarked template would still audit every person
registered, which is the wrong population reported as the right one.

Two rules bind it:

- **mark both branches or neither.** The findings branch and the coverage branch must lose the
  registry together, or `eligible_count` is counted over a population the findings never saw;
- **mark nothing where the registry is the audited population.** `GLOBAL-DQ-009` audits people
  the registry knows and no path reaches, so dropping its registry branch would leave it with
  nothing to audit. It marks no branch, and the runner therefore refuses to strip it.

## Sport parameter file

`SPORTS/params.json` maps a sport slug to its confirmed values:

```json
{
  "BMX": {
    "SPORT_ID": 58,
    "EVENT_PARTICIPANT_TYPE_LIST": "'athlete'",
    "REGISTRY_PARTICIPANT_TYPE_LIST": "'athlete', 'team'",
    "STATISTIC_TYPE_ID": 11,
    "SHARD_ID": 11
  }
}
```

`TOOLS/Run-Query.ps1 -Sport <name>` reads it before querying the database, so a recorded
value is preferred over a discovered one: the file holds evidence, discovery holds a
heuristic. An explicit `-SportId` or `-Params` still overrides both.

The file is data, not documentation. The narrative evidence for each value stays in
`SPORTS/<SportSlug>.md`; `TOOLS/Test-Package.ps1` checks that the two agree on `SPORT_ID`.

## Registry

The `Description` column mirrors each statement's `-- What it does:` comment.

| CheckID | Name | File | Description | Mandatory parameters | Applicability/prerequisite | Category |
|---|---|---|---|---|---|---|
| GLOBAL-DQ-001 | TEMPLATE_NO_TOURNAMENTS_OR_STAGES | `HIERARCHY.sql` | Finds tournament templates with no tournaments, or with tournaments but no stages under any of them. | `SPORT_ID` | Any sport with active templates | NO_RELATED_RECORDS |
| GLOBAL-DQ-002 | TOURNAMENT_STAGE_MISSING_AGE_CLASS | `HIERARCHY.sql` | Finds tournament stages with no age-class relation. | `SPORT_ID` | Sport whose stages are expected to carry an age class (`object_relation 4 -> 151`) | MISSING_VALUES |
| GLOBAL-DQ-003 | TOURNAMENT_STAGE_NO_EVENTS | `HIERARCHY.sql` | Finds tournament stages holding no events. | `SPORT_ID` | Any sport with active stages | NO_RELATED_RECORDS |
| GLOBAL-DQ-004 | TOURNAMENT_STAGE_DATE_RANGE_MISMATCH | `HIERARCHY.sql` | Finds tournament stages whose start or end date does not match the earliest and latest event date inside them. | `SPORT_ID` | Sport whose stage date range is expected to bound its events | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-005 | TOURNAMENT_STAGE_MISSING_START_OR_END_DATE | `HIERARCHY.sql` | Finds tournament stages with a NULL start or end date. | `SPORT_ID` | Any sport with active stages | MISSING_VALUES |
| GLOBAL-DQ-006 | EVENT_MISSING_ROUND_TYPE | `HIERARCHY.sql` | Finds events with no round type, or one that resolves to no round_type row. | `SPORT_ID` | Sport confirmed to use `event.round_typeFK` | MISSING_VALUES |
| GLOBAL-DQ-007 | PARTICIPANT_MISSING_DATE_OF_BIRTH | `PARTICIPANTS.sql` | Finds people carrying no date_of_birth, reached either through the sport registry or through any of the three participation paths: an event participant row, a lineup place or a Comp.Rank row. | `SPORT_ID`, `PERSON_PARTICIPANT_TYPE_LIST`, `SHARD_ID`, `STATISTIC_TYPE_ID` | Any sport that registers or fields people. Scoped to `PERSON_PARTICIPANT_TYPE_LIST` rather than `EVENT_PARTICIPANT_TYPE_LIST`: a date of birth is an attribute of a natural person, so a team entered as an event participant has none and would be reported as missing one in every sport fielding teams. The population is reached through the sport registry and all three participation paths, because no sport uses one mechanism only - a sport entering teams carries its athletes in lineups or in the Comp.Rank statistic, and a check reading the event path alone covers nothing there. A participation count of zero is context and never a reason to omit the row | MISSING_VALUES |
| GLOBAL-DQ-008 | PARTICIPANT_MISSING_PROFILE_FIELDS | `PARTICIPANTS.sql` | Finds participants taking part in at least one event that are missing a name or a country, or - for people only - a first or last name. | `SPORT_ID`, `EVENT_PARTICIPANT_TYPE_LIST`, `PERSON_PARTICIPANT_TYPE_LIST` | Sport confirmed to store split first/last name via `language` types 7 and 8. The row filter stays `EVENT_PARTICIPANT_TYPE_LIST`, because name and country are asserted of every entered participant including a team; the two split-name tests are guarded by `PERSON_PARTICIPANT_TYPE_LIST` in both the reported field list and the emission condition, so a team carrying a name and a country is not reported for lacking a first name it cannot have | MISSING_VALUES |
| GLOBAL-DQ-009 | PARTICIPANT_NO_PARTICIPATION_ANYWHERE | `PARTICIPANTS.sql` | Finds registered participants that take no part in the sport by any of the three paths open to them: an event participant row, a lineup place or a Comp.Rank row. | `SPORT_ID`, `REGISTRY_PARTICIPANT_TYPE_LIST`, `SHARD_ID`, `STATISTIC_TYPE_ID` | Sport with an active `object_participants` registry (`object='sport'`). All three paths are asserted together: a sport that enters teams and carries its people in lineups or in the Comp.Rank statistic would have its whole registry reported by an event-only rule | NO_RELATED_RECORDS |
| GLOBAL-DQ-010 | COMP.RANK_NO_PARTICIPANTS | `STATISTICS.sql` | Finds Comp.Rank holding no participant rows in the sport's shard. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Tournament-owned statistics only (`statistic.object_typeFK=3`); shard confirmed per `DB-SEM-006` | NO_RELATED_RECORDS |
| GLOBAL-DQ-011 | COMP.RANK_SETTINGS_MISSING_START_OR_END_DATE | `STATISTICS.sql` | Finds Comp.Rank with a missing or empty Start date or End date. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_START_DATE_TYPE_ID`, `CONFIG_END_DATE_TYPE_ID` | Tournament-owned statistics only; both config field IDs confirmed for the statistic type | MISSING_VALUES |
| GLOBAL-DQ-012 | COMP.RANK_RESULTS_RANK_INVALID_OR_MISSING | `GLOBAL_DQ\STATISTICS.sql` | Finds Comp.Rank holding a participant whose Rank is not a positive integer, or is missing with no Comment to explain it. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_COMMENT_TYPE_ID` | Tournament-owned statistics only; sport confirmed to store a positive-integer Rank data field and to mark a non-finisher through the Comment data field. A missing or empty Rank is reported only when no Comment explains it, which is how `GLOBAL-DQ-036` already reads the event layer; an invalid value is reported either way | WRONG_RESULTS |
| GLOBAL-DQ-013 | TEMPLATE_MISSING_SET_SUBSET_GENDER_NAME | `HIERARCHY.sql` | Finds tournament templates missing a name, a gender, a subset relation or a resolvable tournament set. | `SPORT_ID` | Sport whose templates are expected to sit in a tournament set through `object_relation 2 -> 152` | MISSING_VALUES |
| GLOBAL-DQ-014 | TEMPLATE_STAGE_GENDER_MISMATCH | `HIERARCHY.sql` | Finds tournament stages whose gender differs from their template's, ignoring mixed and empty on either side. | `SPORT_ID` | Sport where template and stage are expected to agree on gender; mixed is excluded on both sides | WRONG_GENDER |
| GLOBAL-DQ-015 | EVENT_SETTINGS_MISSING_DISCIPLINE | `HIERARCHY.sql` | Finds events with no discipline relation. | `SPORT_ID` | Sport confirmed to carry disciplines on events (`object_discipline` owner type 5) | MISSING_VALUES |
| GLOBAL-DQ-016 | EVENT_SETTINGS_MISSING_GENDER | `HIERARCHY.sql` | Finds events whose parent stage carries no usable gender. | `SPORT_ID` | Sport where the event's gender is carried by its parent stage | MISSING_VALUES |
| GLOBAL-DQ-017 | EVENT_RESULTS_MISSING_FOR_FINISHED | `RESULTS.sql` | Finds finished events (status_descFK=6) where no participant holds a result. | `SPORT_ID` | Sport confirmed to use `finished`/6 and to store results on `event_participants` | WRONG_RESULTS |
| GLOBAL-DQ-018 | EVENT_RESULTS_MEDAL_INVALID_VALUE | `RESULTS.sql` | Finds event-participant Medal values that are not gold, silver or bronze. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID` | Sport confirmed to store a Medal event result restricted to gold/silver/bronze | WRONG_RESULTS |
| GLOBAL-DQ-019 | EVENT_DURATION_FORMAT_MISMATCH_TO_RANK | `RESULTS.sql` | Finds events where a participant's duration breaks the leader/gap convention: rank 1 must be a plain absolute time, every other rank a plus-prefixed gap. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_DURATION_TYPE_ID` | Sport confirmed to follow the leader/gap convention on its duration result type; the accepted time shape is colon-tolerant, so a sport with a stricter shape needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-020 | EVENT_RESULTS_RANK_OUTLIER_ABOVE_FIELD_SIZE | `RESULTS.sql` | Finds event participants in finished events whose Rank exceeds the participant count and is disconnected from the next lower Rank, with no Comment to explain it. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_COMMENT_TYPE_ID` | Sport confirmed to mark non-finishing participants through the Comment result rather than through a rank sentinel alone | WRONG_RESULTS |
| GLOBAL-DQ-021 | EVENT_RESULTS_RANK_DUPLICATE_WITHOUT_COMMENT | `RESULTS.sql` | Finds event participants in finished events sharing a Rank with another where neither row carries a Comment and the values the sport ranks on do not account for the tie, separating a tie whose values disagree from one with no value stored at all. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_COMMENT_TYPE_ID`, `RESULT_TIE_VALUE_TYPE_LIST` | Sport where a shared rank is either explained by a Comment or accounted for by the values in `RESULT_TIE_VALUE_TYPE_LIST` agreeing across the whole tied group. It is no longer restricted to sports that never tie by design: a sport that ties in quantity is now readable, because a tie whose scores or times agree is left alone and only one the values fail to account for is reported. Measured on Artistic Gymnastics, that is the difference between 5192 findings and 162. Every tied row must carry the field, not merely some of them, so a group where one member has no value is still reported — and where no member carries any declared value the finding stands under its own verdict, since nothing justifies the duplicate | WRONG_RESULTS |
| GLOBAL-DQ-022 | COMP.RANK_SETTINGS_MISSING_AGE_CLASS | `STATISTICS.sql` | Finds Comp.Rank with no age-class relation. | `SPORT_ID`, `STATISTIC_TYPE_ID` | Tournament-owned statistics only; sport confirmed to carry `object_relation 83 -> 151` | MISSING_VALUES |
| GLOBAL-DQ-023 | COMP.RANK_SETTINGS_MISSING_DISCIPLINE | `STATISTICS.sql` | Finds Comp.Rank with no discipline relation, or one naming a discipline that does not resolve. | `SPORT_ID`, `STATISTIC_TYPE_ID` | Tournament-owned statistics only; sport confirmed to carry disciplines on statistics (`object_discipline` owner type 83) | MISSING_VALUES |
| GLOBAL-DQ-024 | COMP.RANK_SETTINGS_DATE_RANGE_MISMATCH_STAGE | `STATISTICS.sql` | Finds Comp.Rank whose configured date interval is inverted, or is not contained within the stage dates of its own tournament, separating an inverted interval, one crossing both bounds, a start before the earliest stage and an end after the latest. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_START_DATE_TYPE_ID`, `CONFIG_END_DATE_TYPE_ID` | Tournament-owned statistics only; sport whose statistics are dated inside the span of their tournament's stages. Containment, not endpoint equality, is the invariant: a tournament is often a season and a statistic often covers one stage of it, so a window narrower than the tournament's is the normal case. A sport whose statistic is expected to span its whole tournament exactly needs its own statement. The inverted interval is checked here rather than only in `GLOBAL-DQ-025`, because that one reads the optional Event id config field and so does not apply to every sport | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-025 | COMP.RANK_SETTINGS_DATE_RANGE_MISMATCH_EVENTS | `STATISTICS.sql` | Finds Comp.Rank whose configured date interval is inverted, or does not contain every event it names through the Event id config, separating the four boundary failures. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_START_DATE_TYPE_ID`, `CONFIG_END_DATE_TYPE_ID`, `CONFIG_EVENT_ID_TYPE_ID` | Tournament-owned statistics only; sport confirmed to populate the Event id config field, which is optional today. A configured interval may legitimately begin before the earliest linked event or end after the latest one; containment, not exact endpoint equality, is the invariant | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-026 | COMP.RANK_SETTINGS_MEDAL_SET_INVALID | `STATISTICS.sql` | Finds Comp.Rank whose medal set is not one gold, one silver and one bronze: a type missing, held by more participants than the place takes, or held by fewer - counting relay members who share their team's medal as one holder. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID`, `DATA_TEAM_TYPE_ID` | Tournament-owned statistics only; sport whose statistics of this type award exactly one gold, one silver and one bronze. A sport that awards two bronzes by design ignores `Duplicate_Bronze`; a shared place is separated from a contradicted one rather than merged with it. The statistic layer needs a notion of how many participants hold one place, which the event layer does not, because a Comp.Rank lists a relay athlete by athlete where an event enters the team as one participant: holders are counted per team where the Team data field assigns one, and otherwise the norm is read from the medals present. A sport whose statistic awards a single medal type only leaves the norm undefined by agreement and falls back to the smallest count, which reports nothing | MISSING_VALUES |
| GLOBAL-DQ-027 | COMP.RANK_RESULTS_MEDAL_INVALID_VALUE | `STATISTICS.sql` | Finds Comp.Rank Medal values that are not gold, silver or bronze. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID` | Tournament-owned statistics only; sport confirmed to restrict the Medal data field to gold/silver/bronze | WRONG_RESULTS |
| GLOBAL-DQ-028 | COMP.RANK_RESULTS_TIME_DIFFERENCE_FORMAT_MISMATCH_TO_RANK | `STATISTICS.sql` | Finds Comp.Rank holding a participant whose Time Difference breaks the leader/gap convention: rank 1 a plain absolute time, every other rank a plus-prefixed gap. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_TIME_DIFFERENCE_TYPE_ID` | Tournament-owned statistics only; sport confirmed to follow the leader/gap convention on the Time Difference data field | WRONG_RESULTS |
| GLOBAL-DQ-029 | COMP.RANK_RESULTS_DEPRECATED_DURATION_USED | `STATISTICS.sql` | Finds Comp.Rank still storing a value in the deprecated Duration field, with which of the current Time and Time Difference fields are populated beside it. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_DEPRECATED_DURATION_TYPE_ID`, `DATA_TIME_TYPE_ID`, `DATA_TIME_DIFFERENCE_TYPE_ID` | Tournament-owned statistics only; sport confirmed to have superseded the Duration data field by the Time and Time Difference pair | WRONG_RESULTS |
| GLOBAL-DQ-030 | COMP.RANK_RESULTS_PARTICIPANT_NOT_IN_TOURNAMENT | `STATISTICS.sql` | Finds Comp.Rank participants who are neither an event participant nor a lineup member anywhere under their own tournament. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Tournament-owned statistics only; sport where a ranked athlete may be represented directly or through a team's active lineup inside the same tournament | NO_RELATED_RECORDS |
| GLOBAL-DQ-031 | COMP.RANK_RESULTS_RANK_OUTLIER_ABOVE_FIELD_SIZE | `STATISTICS.sql` | Finds Comp.Rank participants whose Rank exceeds the number ranked and is disconnected from the next lower Rank, with no Comment to explain it. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_COMMENT_TYPE_ID` | Tournament-owned statistics only; sport confirmed to mark non-finishing ranked participants through the Comment data field | WRONG_RESULTS |
| GLOBAL-DQ-032 | COMP.RANK_RESULTS_NO_RANK_DATA_FOR_PARTICIPANTS | `STATISTICS.sql` | Finds Comp.Rank holding participants but no Rank value for any of them, separating one holding no data at all from one holding other data. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID` | Tournament-owned statistics only; sport confirmed to store a Rank data field for every ranked participant | MISSING_VALUES |
| GLOBAL-DQ-033 | COMP.RANK_RESULTS_MISSING_PHASE | `STATISTICS.sql` | Finds Comp.Rank participants carrying no phase row. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID` | Tournament-owned statistics only; sport confirmed to carry a Phase on ranked participants | MISSING_VALUES |
| GLOBAL-DQ-034 | TOURNAMENT_STAGE_MISSING_CORE_FIELDS | `HIERARCHY.sql` | Finds tournament stages missing a name, gender, country or city, carrying a placeholder country, or holding the host country wrongly - absent where the country is International, or present where the country already names a nation. | `SPORT_ID`, `PLACEHOLDER_COUNTRY_LIST` | Any sport with active stages. The host country is conditional, not a fifth mandatory field: it exists to say where a stage belonging to no single nation was actually held, so it is asserted only where the country is `International` and reported as unexpected where the country already names a nation. `International` is therefore excluded from the placeholder test on the stage's own country, though not from the test on the host country, where it names nothing. A sport with no placeholder country row leaves `PLACEHOLDER_COUNTRY_LIST` naming values that simply never match | MISSING_VALUES |
| GLOBAL-DQ-035 | COMP.RANK_SETTINGS_MISSING_CORE_FIELDS | `STATISTICS.sql` | Finds Comp.Rank missing a name, a Gender config, a country or a city relation, or carrying a country that resolves only to a placeholder. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_GENDER_TYPE_ID`, `PLACEHOLDER_COUNTRY_LIST` | Tournament-owned statistics only; all four fields are asserted, as for `GLOBAL-DQ-034` | MISSING_VALUES |
| GLOBAL-DQ-036 | EVENT_RESULTS_RANK_INVALID_OR_MISSING | `RESULTS.sql` | Finds event participants in finished events whose Rank is not a plain positive integer up to the sport's maximum, or is missing with no Comment either, separating a participant holding no result at all from one holding another. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_COMMENT_TYPE_ID`, `RANK_MAX_PLAUSIBLE` | Sport whose largest legitimate field size is recorded in `RANK_MAX_PLAUSIBLE`; a mass-participation sport needs its own value | WRONG_RESULTS |
| GLOBAL-DQ-037 | EVENT_RESULTS_MEDAL_SET_INVALID_FOR_FINAL | `GLOBAL_DQ\RESULTS.sql` | Finds finished Final-round events whose Medal set is not one gold, one silver and one bronze, separating no medals at all, a duplicate contradicted by the place below it, a duplicate shaped like a tie, a duplicated bronze and a missing type. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Sport whose Final-round events award exactly one gold, one silver and one bronze **inside that one event**. A sport that awards two bronzes by design ignores `Duplicate_Bronze`; a shared place is separated from a contradicted one rather than merged with it. A head-to-head sport must not instantiate this: its Final holds two participants, so bronze is not awardable there and every Final is reported as missing it. Where the medal set is split across a Final and its own bronze match, the per-round expectation belongs on the event and the completeness of the set belongs to the Comp.Rank layer, which `GLOBAL-DQ-026` already asserts per statistic. The eligible population is every event whose `status_type` is `finished`, not only the one carrying the plain finished detail: a Final decided in an extra period, after extra time or by an awarded win is still a finished Final, and narrowing to a single detailed status drops it from the audit without the coverage count saying so | MISSING_VALUES |
| GLOBAL-DQ-038 | EVENT_SETTINGS_MISSING_MEDAL_RELATED_FOR_MEDAL_ROUND | `HIERARCHY.sql` | Finds finished events on a medal round type carrying no medal_related property set to yes. | `SPORT_ID`, `MEDAL_ROUND_TYPE_LIST` | Sport confirmed to carry the `medal_related` event property. Keyed on every round that awards a medal rather than on the Final alone, so a sport deciding bronze in its own match asserts the property there too. The eligible population is every event whose `status_type` is `finished`, not only the one carrying the plain finished detail | MISSING_VALUES |
| GLOBAL-DQ-039 | EVENT_RESULTS_UNEXPECTED_MEDAL_FOR_NON_MEDAL_ROUND | `RESULTS.sql` | Finds finished events off a medal round type that carry a Medal result. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `MEDAL_ROUND_TYPE_LIST` | Sport that awards medals only on the rounds recorded in `MEDAL_ROUND_TYPE_LIST`. The list is what makes the check correct for a sport whose bronze is decided outside the Final; keyed on the Final alone it reports every bronze match. The eligible population is every event whose `status_type` is `finished`, not only the one carrying the plain finished detail | WRONG_RESULTS |
| GLOBAL-DQ-040 | EVENT_FINAL_WITHOUT_COMP.RANK | `STATISTICS.sql` | Finds Final-round events that no Comp.Rank under their own tournament names through its Event id config, separating a tournament holding none at all from one whose Comp.Rank declares no event scope. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_EVENT_ID_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Sport where every Final event is expected to have its own statistic, per `DB-SEM-013`; the Event id config field is optional today, so the two non-finding branches carry that fact | NO_RELATED_RECORDS |
| GLOBAL-DQ-041 | COMP.RANK_RESULTS_MEDAL_ON_NON_MEDAL_ROUND_PHASE | `STATISTICS.sql` | Finds Comp.Rank participants carrying a Medal while their phase names a round the sport awards no medals on. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID`, `MEDAL_ROUND_TYPE_LIST` | Tournament-owned statistics only; sport confirmed to carry a Phase and to award medals only on the rounds recorded in `MEDAL_ROUND_TYPE_LIST`. The statistic-layer twin of `GLOBAL-DQ-039` and keyed on the same list, so a bronze phase is not reported for a sport that decides bronze outside the Final | WRONG_RESULTS |
| GLOBAL-DQ-042 | EVENT_FINAL_PARTICIPANT_NOT_IN_COMP.RANK | `STATISTICS.sql` | Finds participants in Final-round events who appear in none of the Comp.Rank naming their event. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `CONFIG_EVENT_ID_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Sport where a Final participant is expected to appear in the statistic covering that event; empty statistics are excluded so the finding is not a restatement of `GLOBAL-DQ-010` | NO_RELATED_RECORDS |
| GLOBAL-DQ-043 | EVENT_PARTICIPANTS_GENDER_MISMATCH | `PARTICIPANTS.sql` | Finds event participants whose gender contradicts their stage or their own lineup: an athlete or a team against a single-gender stage, a mixed team whose lineup misses a gender, or a single-gender team holding the other. | `SPORT_ID` | Sport whose stage gender is authoritative for its participants; the lineup branches apply only where a team entry has at least one active lineup row | WRONG_GENDER |
| GLOBAL-DQ-044 | COMP.RANK_RESULTS_GENDER_MISMATCH | `STATISTICS.sql` | Finds Comp.Rank whose Gender config does not match the gender of its own participants. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `CONFIG_GENDER_TYPE_ID` | Tournament-owned statistics only; sport confirmed to populate the Gender config field. Distinguishes team-holding from athlete-holding statistics structurally, not by name | WRONG_GENDER |
| GLOBAL-DQ-045 | EVENT_DURATION_FULL_TIME_MISMATCH_TO_RANK | `GLOBAL_DQ\RESULTS.sql` | Finds finished events in the sport's timed disciplines where a participant's full time is missing despite a Rank, present without one, badly formatted, or zero. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `TIMED_DISCIPLINE_LIST` | Sport with at least one timed discipline; judged disciplines must be left out of `TIMED_DISCIPLINE_LIST` or every judged participant is reported. A negative value fails the shape test and is reported as an invalid format, so the zero-time branch is what a plain `0` needs | WRONG_RESULTS |
| GLOBAL-DQ-046 | COMP.RANK_RESULTS_TIME_FULL_TIME_MISMATCH_TO_RANK | `GLOBAL_DQ\STATISTICS.sql` | Finds Comp.Rank in the sport's timed disciplines whose time storage contradicts a Rank: a Time or Time Difference missing where the rank calls for one, either present with no rank, or a Time badly formatted or zero. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_TIME_TYPE_ID`, `DATA_TIME_DIFFERENCE_TYPE_ID`, `TIMED_DISCIPLINE_LIST` | Tournament-owned statistics only; the discipline list is read from the statistic layer (`object_discipline` owner type 83), which may cover a different population than the event layer. The leader carries no gap to anybody, so rank 1 is exempt from the Time Difference branch; without that exemption every winner in the sport is reported | WRONG_RESULTS |
| GLOBAL-DQ-047 | EVENT_RESULTS_UNEXPECTED_FOR_NOT_STARTED | `RESULTS.sql` | Finds events in a not-started status that carry a result. | `SPORT_ID`, `NOT_STARTED_DESC_LIST` | Sport whose not-started detailed statuses all mean the event has not run; a status used for abandoned events must be left out of the list | WRONG_RESULTS |
| GLOBAL-DQ-048 | TOURNAMENT_STAGE_NAME_FORMAT_INVALID | `HIERARCHY.sql` | Finds tournament-stage names breaking a text-hygiene rule - spacing, control or corrupted characters, hyphenation, capitalisation, a placeholder or a numeric-only name - one row per name, naming every rule it breaks. | `SPORT_ID` | Any sport with named active stages; `HYPHEN_WITHOUT_SPACES` reports a compound proper noun (`Saint-Denis`, `U-23`) and `NON_ASCII_CHARACTER` reports a legitimate diacritic, so both are named separately rather than merged into one verdict | MALFORMED_NAME |
| GLOBAL-DQ-049 | EVENT_NAME_FORMAT_INVALID | `HIERARCHY.sql` | Finds event names breaking a text-hygiene rule - spacing, control or corrupted characters, hyphenation, capitalisation, a placeholder or a numeric-only name - one row per name, naming every rule it breaks. | `SPORT_ID` | Any sport with named active events; the same two noisy rules as `GLOBAL-DQ-048` apply, and a sport whose event names are codes rather than prose will report its whole vocabulary | MALFORMED_NAME |
| GLOBAL-DQ-050 | TOURNAMENT_STAGE_NAME_CASE_INCONSISTENT | `HIERARCHY.sql` | Finds tournament-stage name spellings that lose to a more common spelling of the same name, identical but for case and spacing, with the dominant spelling beside it. | `SPORT_ID` | Any sport with named active stages; where two spellings occur equally often no spelling can be called dominant, so both are reported under their own check_type and the reviewer chooses | MALFORMED_NAME |
| GLOBAL-DQ-051 | COMP.RANK_NAME_FORMAT_INVALID | `STATISTICS.sql` | Finds Comp.Rank names breaking a text-hygiene rule - spacing, control or corrupted characters, hyphenation, capitalisation, a placeholder or a numeric-only name - one row per name, naming every rule it breaks. | `SPORT_ID`, `STATISTIC_TYPE_ID` | Tournament-owned statistics only; the same two noisy rules as `GLOBAL-DQ-048` apply | MALFORMED_NAME |
| GLOBAL-DQ-052 | EVENT_RESULTS_COMMENT_INVALID_OR_CONTRADICTED | `RESULTS.sql` | Finds Comment values outside the sport's status codes, or marking a participant as unclassified while a Rank, a time or a Medal is stored for that same participant. | `SPORT_ID`, `RESULT_COMMENT_TYPE_ID`, `RESULT_COMMENT_VALUE_LIST`, `RESULT_COMMENT_NO_RESULT_LIST`, `RESULT_RANK_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `RESULT_DURATION_TYPE_ID`, `RESULT_MEDAL_TYPE_ID` | Sport whose Comment result resolves to a closed set of status codes recorded in `RESULT_COMMENT_VALUE_LIST`; a sport storing free text there would report its whole population. `RESULT_COMMENT_NO_RESULT_LIST` holds every spelling that means the participant has no classified result, including one the value list rejects, so a contradiction is found regardless of how the status is spelled | WRONG_RESULTS |
| GLOBAL-DQ-053 | EVENT_RESULTS_MEDAL_RANK_MISMATCH | `RESULTS.sql` | Finds event participants whose Medal does not match the place it stands for, or that carry no Rank at all. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `RESULT_RANK_TYPE_ID` | Sport awarding gold, silver and bronze to places one, two and three; a tie is unaffected because each row is compared with its own Rank, but a sport mapping medals to other places needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-054 | EVENT_RESULTS_RANK_FULL_TIME_NOT_MONOTONIC | `RESULTS.sql` | Finds events in the sport's timed disciplines holding a pair whose Rank order contradicts their full-time order, so a better-ranked competitor is recorded as slower. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `TIMED_DISCIPLINE_LIST` | Sport with at least one timed discipline whose finishing order follows the clock; a discipline ranked by judging or by points must be left out of `TIMED_DISCIPLINE_LIST` | WRONG_RESULTS |
| GLOBAL-DQ-055 | EVENT_PARTICIPANTS_DUPLICATE_IN_EVENT | `RESULTS.sql` | Finds events where one participant holds more than one participant row, so the same competitor is entered twice. | `SPORT_ID` | Any sport where a competitor takes part once per event; a format entering the same participant twice by design needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-056 | EVENT_DURATION_FULL_TIME_ARITHMETIC_MISMATCH | `RESULTS.sql` | Finds events in the sport's timed disciplines where a participant's full time does not equal the leader's full time plus their own gap, beyond the sport's tolerance. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_DURATION_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `TIMED_DISCIPLINE_LIST`, `FULL_TIME_TOLERANCE_SECONDS` | Sport confirmed to follow the leader/gap convention and to store an absolute full time beside it; `FULL_TIME_TOLERANCE_SECONDS` must match the precision of the source, and a value written in a shape the parser does not read is left out rather than reported | WRONG_RESULTS |
| GLOBAL-DQ-057 | COMP.RANK_RESULTS_COMMENT_INVALID_OR_CONTRADICTED | `STATISTICS.sql` | Finds Comp.Rank Comment values outside the sport's status codes, or marking a participant as unclassified while a Rank, a Time or a Medal is stored for that same participant. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_COMMENT_TYPE_ID`, `DATA_COMMENT_VALUE_LIST`, `DATA_COMMENT_NO_RESULT_LIST`, `DATA_RANK_TYPE_ID`, `DATA_TIME_TYPE_ID`, `DATA_MEDAL_TYPE_ID` | Tournament-owned statistics only; the statistic layer is inventoried separately from the event layer, because the two are not guaranteed to share a vocabulary even within one sport | WRONG_RESULTS |
| GLOBAL-DQ-058 | EVENT_TEAM_PARTICIPANT_WITHOUT_LINEUP | `PARTICIPANTS.sql` | Finds events whose team participants reach no lineup, separating an event where no team does from one where only some do. | `SPORT_ID` | Sport that fields teams in events; a sport whose event participants are all individuals has no eligible population and its coverage count says so rather than the check reading clean | NO_RELATED_RECORDS |
| GLOBAL-DQ-059 | EVENT_RESULTS_DUPLICATE_ROWS | `RESULTS.sql` | Finds events holding more than one result row for the same participant and result type, separating a duplicate repeating the value from one storing a conflicting one. | `SPORT_ID` | Sport confirmed to store one active result row per participant and result type; a format that legitimately records the same result type twice for one participant needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-060 | COMP.RANK_RESULTS_DUPLICATE_ROWS | `STATISTICS.sql` | Finds Comp.Rank holding more than one data row for the same participant and field, separating a duplicate repeating the value from one storing a conflicting one. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Tournament-owned statistics only; shard confirmed per `DB-SEM-006`. The statistic layer is checked separately from the event layer because the two store their values in unrelated tables | WRONG_RESULTS |
| GLOBAL-DQ-061 | EVENT_STATUS_TIME_CONFLICT | `HIERARCHY.sql` | Finds events whose status contradicts their own date: finished but dated in the future, or not started and older than the sport's staleness window. | `SPORT_ID`, `NOT_STARTED_DESC_LIST`, `STALE_NOT_STARTED_DAYS` | Sport whose events carry a start date and whose not-started detailed statuses all mean the event has not run. `STALE_NOT_STARTED_DAYS` is a judgement value rather than a structural fact, and it is deliberately the same for every sport - `2` by decision of 2026-08-06 - because the window measures how long a status went unmaintained after the event's own date, which is a property of the feed rather than of the sport. A sport wanting its own value has to say why | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-062 | EVENT_DUPLICATE_BY_METADATA | `HIERARCHY.sql` | Finds events sharing a stage, discipline, calendar day and participant set, separating an exact timestamp match from a same-day one, and empty shells from a repeated competition. | `SPORT_ID`, `DUPLICATE_KEY_INCLUDES_EVENT_NAME`, `DUPLICATE_KEY_USES_CALENDAR_DAY` | Any sport with named, dated events. The participant set is always part of the key, precisely because a heats-based sport runs several identically named events in one slot; without it every such heat is reported. Two parts of the key are the sport's own facts and are declared rather than assumed, because getting either wrong turns the check into noise one way or silence the other. `DUPLICATE_KEY_INCLUDES_EVENT_NAME` is 0 for a sport naming its events after the competitors, where the name repeats the participant set in an arbitrary order and splits `A-B` from `B-A` that the participant key has already matched. `DUPLICATE_KEY_USES_CALENDAR_DAY` is 1 where one meeting happens at most once a day, which catches the re-import that arrived with a shifted kick-off, and 0 for a sport running the same field several times a day - three motos under one name - where only the exact moment separates them and a day key reports the whole heat structure | WRONG_RESULTS |
| GLOBAL-DQ-063 | TOURNAMENT_NO_STAGES | `HIERARCHY.sql` | Finds tournaments holding no stages, including those whose template holds other populated tournaments and so never reaches GLOBAL-DQ-001. | `SPORT_ID` | Any sport with active tournaments. Overlaps `GLOBAL-DQ-001` wherever a template is empty throughout; what it adds is the empty tournament inside an otherwise populated template, which the template-level check cannot see | NO_RELATED_RECORDS |
| GLOBAL-DQ-064 | COMP.RANK_ATHLETE_TEAM_MISSING_OR_INVALID | `STATISTICS.sql` | Finds athletes inside a Comp.Rank that uses the Team field whose own Team value is absent, does not resolve to a team, or is stored twice - repeating a value or contradicting itself. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_TEAM_TYPE_ID` | Tournament-owned statistics only; sport confirmed to store the athlete's team in the Team data field as a `participant.id`. The eligible population is the statistic that uses the field at all, so an individual-event statistic is excluded rather than reported as violating | MISSING_VALUES |
| GLOBAL-DQ-065 | COMP.RANK_TEAM_ATHLETE_COUNT_UNEVEN | `STATISTICS.sql` | Finds Comp.Rank whose teams do not all field the same number of athletes, separating a shortfall of one athlete from a larger one. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_TEAM_TYPE_ID` | Tournament-owned statistics only; sport whose teams field an identical number of athletes in one competition. A shortfall of one is separated from a larger one because a single missing athlete is often a real withdrawal, while a larger gap is almost always incomplete entry | WRONG_RESULTS |
| GLOBAL-DQ-066 | COMP.RANK_TEAM_GENDER_BALANCE_UNEVEN | `STATISTICS.sql` | Finds mixed Comp.Rank holding a team whose athletes are not an equal number of male and female, separating a team fielding one gender only from one fielding both unevenly. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_TEAM_TYPE_ID`, `CONFIG_GENDER_TYPE_ID` | Tournament-owned statistics only; sport whose mixed formats require a symmetric team composition. Measures each team on its own, which `GLOBAL-DQ-044` cannot do: a statistic holding both genders overall passes there while one of its teams fields men only | WRONG_GENDER |
| GLOBAL-DQ-067 | EVENT_MIXED_TEAM_LINEUP_GENDER_BALANCE_UNEVEN | `PARTICIPANTS.sql` | Finds mixed-gender teams whose lineup holds male and female members in unequal numbers. | `SPORT_ID` | Sport whose mixed team formats require a symmetric lineup. A lineup missing one gender entirely is excluded so the finding is not a restatement of `GLOBAL-DQ-043`, which already reports it | WRONG_GENDER |
| GLOBAL-DQ-068 | EVENT_TEAM_LINEUP_SIZE_UNEVEN | `PARTICIPANTS.sql` | Finds events whose teams do not all field the same number of lineup members, separating a shortfall of one member from a larger one. | `SPORT_ID` | Sport that fields teams in events. Measures each event against itself, so a sport running several team formats under one discipline is not reported for the difference between formats; a format that legitimately fields uneven teams inside one event needs its own statement. A team with no lineup at all is outside the eligible population, so the finding is not a restatement of `GLOBAL-DQ-058` | WRONG_RESULTS |
| GLOBAL-DQ-069 | EVENT_RESULTS_VALUE_BLANK | `GLOBAL_DQ\RESULTS.sql` | Finds result values that are neither empty nor readable, made only of ordinary spacing or only of invisible characters, separating the two. | `SPORT_ID` | Any sport storing results on `event_participants`. `NULL` and `''` are one state in `DATABASE.md`, the active empty row, which a sport uses to record that a field does not apply, so neither is a finding. What is asserted is narrower: a value that is neither of them must carry content. Ordinary spacing is invisible to every other value check, findings and coverage alike, because they filter on `TRIM(value) <> ''`; an invisible character survives `TRIM` and reaches them as a false value | MISSING_VALUES |
| GLOBAL-DQ-070 | COMP.RANK_RESULTS_VALUE_BLANK | `GLOBAL_DQ\STATISTICS.sql` | Finds Comp.Rank data values that are neither empty nor readable, made only of ordinary spacing or only of invisible characters, separating the two. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Tournament-owned statistics only; shard confirmed per `DB-SEM-006`. Carries the same class boundary as `GLOBAL-DQ-069` and is inventoried separately from it, because the two layers store their values in unrelated tables. The empty value is out of scope for a documented reason: a Comp.Rank field set is declared per statistic type, so a field that does not apply to a participant is stored empty rather than omitted | MISSING_VALUES |
| GLOBAL-DQ-071 | EVENT_NO_PARTICIPANTS | `PARTICIPANTS.sql` | Finds events holding no participant at all, separating a finished event from an unfinished one, with the soft-deleted participant rows it still carries. | `SPORT_ID` | Any sport with active events. A finished event holding no participant already reaches `GLOBAL-DQ-017`, whose `NOT EXISTS` over results is vacuously true for it, but under the diagnosis that its results are missing; this statement names the actual cause and adds the event that has not finished, which no check sees today | NO_RELATED_RECORDS |
| GLOBAL-DQ-072 | COMP.RANK_RESULTS_MEDAL_RANK_MISMATCH | `GLOBAL_DQ\STATISTICS.sql` | Finds Comp.Rank participants whose Medal does not match the place it stands for, or that carry no Rank at all. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID`, `DATA_RANK_TYPE_ID` | Tournament-owned statistics only; sport awarding gold, silver and bronze to places one, two and three. A value outside those three resolves to no expected rank and is left to `GLOBAL-DQ-027`, so one row is never reported twice under different verdicts. This is the statistic-layer twin of `GLOBAL-DQ-053`, which cannot see a Comp.Rank medal | WRONG_RESULTS |
| GLOBAL-DQ-073 | EVENT_SETTINGS_UNEXPECTED_MEDAL_RELATED_FOR_NON_MEDAL_ROUND | `GLOBAL_DQ\HIERARCHY.sql` | Finds finished events off a medal round type carrying a medal_related property set to yes. | `SPORT_ID`, `MEDAL_ROUND_TYPE_LIST` | Sport that awards medals only on the rounds recorded in `MEDAL_ROUND_TYPE_LIST`, so the property marks a medal round; the mirror of `GLOBAL-DQ-038` and keyed on the same list. A round awarding a medal outside that list is reported here exactly as `GLOBAL-DQ-039` reports it for the Medal result. The eligible population is every event whose `status_type` is `finished`, not only the one carrying the plain finished detail | WRONG_RESULTS |
| GLOBAL-DQ-074 | EVENT_MISSING_VENUE | `GLOBAL_DQ\HIERARCHY.sql` | Finds events resolving to no venue, separating no venue link at all from a link naming a venue that does not resolve. | `SPORT_ID` | Any sport that records where its events are held. `venue_object` is the only mechanism attaching a venue to a hierarchy object, per `DATABASE.md`, so a sport that attaches none reports its whole population and the coverage count says exactly that rather than the check reading clean. The tournament stage is read alongside the event, because a championship held at one site records the venue once on the stage | MISSING_VALUES |
| GLOBAL-DQ-075 | EVENT_ROUND_TYPE_NOT_IN_EXPECTED_SET | `GLOBAL_DQ\HIERARCHY.sql` | Finds events whose round type is outside the set the sport is confirmed to contest. | `SPORT_ID`, `ROUND_TYPE_LIST` | Sport whose contested rounds are inventoried in `ROUND_TYPE_LIST`. The list is a judgement about which rounds the sport legitimately runs, not an inventory of what it currently stores, so the check normally reports nothing on the day it is written and exists to catch the value entered afterwards. A NULL or dangling `round_typeFK` belongs to `GLOBAL-DQ-006` and is excluded here by the join to `round_type` | WRONG_RESULTS |
| GLOBAL-DQ-076 | EVENT_RESULTS_NUMERIC_FIELD_NON_NUMERIC | `GLOBAL_DQ\RESULTS.sql` | Finds non-numeric values in the sport's numeric result fields, separating one of the sport's own status codes, a no-data sentinel such as nan, a number written with thousands separators, and any other text. | `SPORT_ID`, `NUMERIC_RESULT_TYPE_LIST`, `RESULT_COMMENT_VALUE_LIST` | Sport with at least one result type that carries a measured quantity, listed in `NUMERIC_RESULT_TYPE_LIST`. A time or duration field must be left out: its value is not a plain number and every row would be reported. The status vocabulary is read so a leaked status code is named as one rather than as unclassified text | WRONG_RESULTS |
| GLOBAL-DQ-077 | COMP.RANK_RESULTS_NUMERIC_FIELD_NON_NUMERIC | `GLOBAL_DQ\STATISTICS.sql` | Finds non-numeric values in the sport's numeric Comp.Rank fields, separating one of the sport's own status codes, a no-data sentinel such as nan, a number written with thousands separators, and any other text. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `NUMERIC_DATA_TYPE_LIST`, `DATA_COMMENT_VALUE_LIST` | Tournament-owned statistics only; sport with at least one numeric data field, listed in `NUMERIC_DATA_TYPE_LIST`. The Time and Time Difference fields must be left out, as must Medal and Comment, which are text by design. A sport whose only numeric fields are Rank and Team gains nothing here, because `GLOBAL-DQ-012` and `GLOBAL-DQ-064` already assert both. The field must also carry values outside IOC-purpose templates, which the statement excludes; a numeric field confined to them leaves the eligible count at zero by construction, which is misdirected scope rather than clean data | WRONG_RESULTS |
| GLOBAL-DQ-078 | TOURNAMENT_NAME_FORMAT_INVALID | `GLOBAL_DQ\HIERARCHY.sql` | Finds tournament names breaking a text-hygiene rule - spacing, control or corrupted characters, hyphenation, capitalisation, a placeholder, or a numeric-only name that is not a bare season year - one row per name, naming every rule it breaks. | `SPORT_ID` | Any sport with named active tournaments, excluding IOC-purpose templates. The same two noisy rules as `GLOBAL-DQ-048` apply. A bare four-digit year is accepted as a whole name, unlike on a stage, because naming a tournament for its season alone is the norm in this database | MALFORMED_NAME |
| GLOBAL-DQ-079 | TEMPLATE_NAME_FORMAT_INVALID | `GLOBAL_DQ\HIERARCHY.sql` | Finds tournament template names breaking a text-hygiene rule - spacing, control or corrupted characters, hyphenation, capitalisation, a placeholder or a numeric-only name - one row per name, naming every rule it breaks. | `SPORT_ID` | Any sport with named active templates, excluding IOC-purpose templates. The same two noisy rules as `GLOBAL-DQ-048` apply. The population is small, one row per template rather than per tournament, so a finding here is worth more than its count suggests: every tournament under the template inherits the name | MALFORMED_NAME |
| GLOBAL-DQ-080 | TOURNAMENT_NAME_SEASON_CONTRADICTS_DATES | `HIERARCHY.sql` | Finds tournaments whose name disagrees with the calendar years their stages occupy, separating a season span against a single year, a single year against a span, years the stages never reach, and stages crossing more than two years. | `SPORT_ID` | Sport whose tournament names carry the season they were held in. The season is read from the stages, which is what defines it: an event dated outside its own stage is `GLOBAL-DQ-004`, not a naming defect. A tournament whose name carries no year at all is out of scope, because there is no label to contradict. The two-digit span form `2025/26` is recognised as a span, but the year it abbreviates is not matched against the stages, so a sport using that form should record the full form or accept that only the first year is verified | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-081 | TEMPLATE_TOURNAMENT_YEAR_GAP | `HIERARCHY.sql` | Finds tournament templates whose editions break the template's own rhythm, so a four-yearly series is measured against four years, and a gap is not counted for a year the sport is recorded as not having run. | `SPORT_ID`, `SERIES_MIN_YEARS`, `SERIES_SKIP_YEARS` | Sport running at least one series long enough for a break to mean something, recorded in `SERIES_MIN_YEARS`. The rhythm is measured per template, so an every-fourth-year series is judged against four years and a break of eight reports one edition skipped rather than seven. A template whose intervals are evenly split between two values takes the smaller as its rhythm, which reports less. Absence is still not provable from inside the database: a cancelled edition is a true break in the world and is reported as one, which is why `SERIES_SKIP_YEARS` exists for the years the sport is known not to have run | NO_RELATED_RECORDS |
| GLOBAL-DQ-082 | TOURNAMENT_STAGE_EVENT_DISCIPLINE_INCONSISTENT | `HIERARCHY.sql` | Finds tournament stages whose own events do not agree on a discipline. | `SPORT_ID` | Sport whose tournament stage groups a single discipline, confirmed before the check is run. A sport that legitimately runs several disciplines under one stage reports its whole population and must not instantiate this. It asks whether a discipline is right, where `GLOBAL-DQ-015` asks only whether one is present; the stage makes that answerable without a vocabulary, because its own events disagree with each other rather than with a list somebody had to write | WRONG_RESULTS |
| GLOBAL-DQ-083 | EVENT_PARTICIPANT_COUNT_NOT_TWO | `PARTICIPANTS.sql` | Finds head-to-head events holding participants but not exactly two, separating fewer than the pair from more. | `SPORT_ID` | Sport whose competition model is `H2H` per `DATABASE.md` `DB-SEM-015`, where an event is a contest between exactly two entries. An event holding no participant at all is `GLOBAL-DQ-071` and is deliberately not restated here, so the two do not report the same event twice | WRONG_STRUCTURE |
| GLOBAL-DQ-084 | EVENT_RESULT_SCORE_TIED | `RESULTS.sql` | Finds head-to-head events whose two participants hold an identical deciding score, so no winner can be read, separating a tie on a zero score from one on a played score. | `SPORT_ID`, `RESULT_FINAL_SCORE_TYPE_ID` | Sport whose competition model is `H2H` and whose format admits no draw; a sport that does admit one records that instead of instantiating this. A tie on zero is separated because it reads as an unplayed or unimported event rather than as a scoring defect | WRONG_RESULTS |
| GLOBAL-DQ-085 | EVENT_SCOPE_PERIOD_SUM_MISMATCH_TOTAL | `RESULTS.sql` | Finds participants whose period-by-period scope values do not add up to their deciding score, separating a total above the sum of its periods from one below it. | `SPORT_ID`, `SCOPE_TYPE_ID`, `SCOPE_PERIOD_DATA_TYPE_LIST`, `RESULT_FINAL_SCORE_TYPE_ID` | Sport storing a score both as a total on the result row and period by period in the scope layer, where the total is the sum of the periods by definition. A sport whose periods are not summands of the total - a set-based score, a checkpoint split - must not instantiate this. Only plainly numeric values are summed, so a period holding text is left to the value-shape checks rather than silently counted as zero | WRONG_RESULTS |
| GLOBAL-DQ-086 | EVENT_SCOPE_PERIOD_VALUE_UNRECOGNISED | `RESULTS.sql` | Finds scope period values that are neither a non-negative number nor one of the sport's sentinels, separating empty active rows, negative numbers and unrecognised tokens. | `SPORT_ID`, `SCOPE_TYPE_ID`, `SCOPE_PERIOD_DATA_TYPE_LIST`, `SCOPE_PERIOD_SENTINEL_LIST` | Sport storing a score period by period in the scope layer and having a confirmed vocabulary for a period that was not played. The sentinel list is what the sport is confirmed to use, so a symbol outside it is reported before it reaches an arithmetic check as a silent zero; a sport with no sentinel records the empty list. Guards `GLOBAL-DQ-085`, which sums only plainly numeric values and would otherwise leave an unknown token invisible - and a negative period, which passes every numeric test and would be summed as real, is named here for the same reason. A sport whose period legitimately holds a negative figure must not instantiate this | WRONG_RESULTS |
| GLOBAL-DQ-087 | EVENT_WINNER_MISSING_OR_INVALID | `HIERARCHY.sql` | Finds finished head-to-head events with no Winner property, or one whose value is empty or outside the side vocabulary the sport uses. | `SPORT_ID`, `WINNER_VALUE_LIST` | Sport whose competition model is `H2H` per `DATABASE.md` `DB-SEM-015` and which records the winning side as the `Winner` event property. The vocabulary is per sport and is not one set: `Home`/`Away`, `a`/`b` and `A`/`B` are all in use, several sports mix two of them, and a void or undecided contest is recorded as `x` where the sport uses one. A sport that stores no Winner at all cannot instantiate this until it does | MISSING_VALUES |
| GLOBAL-DQ-088 | EVENT_WINNER_CONTRADICTS_SCORE | `RESULTS.sql` | Finds finished head-to-head events whose Winner names the lower-scoring side, or names a side at all while the two scores are equal. | `SPORT_ID`, `RESULT_FINAL_SCORE_TYPE_ID`, `WINNER_HOME_VALUE_LIST`, `WINNER_AWAY_VALUE_LIST` | Sport whose competition model is `H2H`, which records a `Winner` event property and decides the contest on a single numeric score. `event_participants.number` 1 is the home side and 2 the away side. A sport whose deciding figure is not that score - sets won rather than points, a judged decision - must not instantiate this against the point score. A void marker outside the two side lists is left to `GLOBAL-DQ-087` rather than read as a contradiction | WRONG_RESULTS |
| GLOBAL-DQ-089 | EVENT_EXTRA_PERIOD_STATUS_MISMATCH | `HIERARCHY.sql` | Finds events whose extra period and detailed status disagree: a period scored while the status does not say the event went to one, or the status saying it did with no period scored. | `SPORT_ID`, `SCOPE_TYPE_ID`, `SCOPE_EXTRA_PERIOD_DATA_TYPE_ID`, `EXTRA_PERIOD_STATUS_DESC_LIST` | Sport storing its extra period as its own scope column and marking it with a detailed status. Whether the extra period was played is read from that column and never from a count of scored periods: a period count cannot separate an extra period from a contest that ended early, because both leave the same number of scored periods behind. A status describing how the event ended rather than whether it went to an extra period - an awarded win - occupies the same field, so a sport that uses both will report the overlap here until the status set records it | WRONG_STRUCTURE |
| GLOBAL-DQ-090 | EVENT_RESULT_MIRRORED_SCORE_TYPES_DISAGREE | `RESULTS.sql` | Finds participants whose two mirrored score types disagree, separating a pair holding different values from one where a side of the pair is absent. | `SPORT_ID`, `RESULT_FINAL_SCORE_TYPE_ID`, `RESULT_MIRROR_SCORE_TYPE_ID` | Sport confirmed to store one figure in two result types, so that the pair is a duplicate by design and any disagreement is a defect. Two types that hold related but distinct figures - a score after regulation against a score after an extra period, points against sets - are not a mirrored pair and must not instantiate this. Either side being absent is reported as its own finding rather than skipped, because a duplicate that stopped being written is the same defect caught earlier | WRONG_RESULTS |
| GLOBAL-DQ-091 | EVENT_SCOPE_PERIOD_NOT_STORED_FOR_BOTH_SIDES | `RESULTS.sql` | Finds events where a period is stored for some participants but not all, or twice for one, so the two sides disagree about which periods exist. | `SPORT_ID`, `SCOPE_TYPE_ID`, `SCOPE_PERIOD_DATA_TYPE_LIST` | Sport storing a score period by period in the scope layer, where every participant of an event contests the same periods. Judged against the event's own participant count rather than a fixed pair, so an event holding other than the usual number of entries is not reported twice: that defect is `GLOBAL-DQ-083`. A period no participant stores at all is silent by construction, which is what separates an unplayed period from a disagreement. Adds what `GLOBAL-DQ-085` cannot see: that check sums each participant separately, so a side missing a whole period simply reaches a smaller total that agrees with itself | WRONG_STRUCTURE |
| GLOBAL-DQ-092 | EVENT_SCOPE_PERIOD_SENTINEL_NOT_TRAILING | `RESULTS.sql` | Finds events whose unplayed-period sentinel is contradicted by its neighbours: a scored period after a participant's own first sentinel, or one side marking a period unplayed while the other scores it. | `SPORT_ID`, `SCOPE_TYPE_ID`, `SCOPE_PERIOD_DATA_TYPE_LIST`, `SCOPE_PERIOD_SENTINEL_LIST` | Sport storing a score period by period in the scope layer and having a confirmed sentinel for a period that was not played, recorded in `SCOPE_PERIOD_SENTINEL_LIST`; a sport with no sentinel has an empty eligible population and its coverage count says so. Playing order is read from the ascending period data type id, so a sport whose ids do not ascend in playing order must not instantiate this. Asserts the sentinel's meaning where `GLOBAL-DQ-086` asserts only its vocabulary: a symbol outside the list is reported there, a symbol in the wrong place is reported here | WRONG_RESULTS |
| GLOBAL-DQ-093 | EVENT_RESULTS_MEDAL_SET_INVALID_FOR_MEDAL_ROUND | `RESULTS.sql` | Finds finished medal-round events whose Medal set is not what the round decides - gold and silver on a Final, bronze on a bronze match - separating no medal at all, a medal the round does not decide, a repeated type and a missing one. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `FINAL_ROUND_TYPE_LIST`, `BRONZE_ROUND_TYPE_LIST` | Head-to-head sport per `DB-SEM-015` that decides bronze in its own match rather than inside the Final, so no single event holds the full medal set. This is what `GLOBAL-DQ-037` cannot express: that check asserts all three medals of one event and reports every Final of such a sport as missing bronze. A sport awarding every medal in one event uses `GLOBAL-DQ-037` instead and must not instantiate this. The detailed status is not narrowed beyond `finished`, so a Final decided in an extra period or awarded stays in the audit | WRONG_RESULTS |
| GLOBAL-DQ-094 | EVENT_RESULTS_MEDAL_CONTRADICTS_SCORE | `RESULTS.sql` | Finds participants on a medal round whose Medal does not match the place their own score gives them, separating a missing medal, one stored where the round awards none, and one naming the wrong place. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `RESULT_FINAL_SCORE_TYPE_ID`, `FINAL_ROUND_TYPE_LIST`, `BRONZE_ROUND_TYPE_LIST` | Head-to-head sport deciding its medal matches on a single numeric score. The place is read from the pair of scores rather than from a Rank or a Winner property, which is what makes it instantiable for a sport storing neither - `GLOBAL-DQ-053` needs a Rank result and cannot run there. A tie is excluded rather than judged, because no place can be read from it; `GLOBAL-DQ-084` names it. A sport whose deciding figure is not that score - sets won, a judged decision - must not instantiate this | WRONG_RESULTS |
| GLOBAL-DQ-095 | COMP.RANK_RESULTS_RANK_DUPLICATE_WITHOUT_COMMENT | `STATISTICS.sql` | Finds a Comp.Rank place held by more participants than its teams field athletes, with no Comment to explain it, while the place that sharing consumes is still awarded - so the place is occupied twice over rather than being a joint place the ranking stops at. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_COMMENT_TYPE_ID`, `DATA_TEAM_TYPE_ID` | Tournament-owned statistics only. The statistic-layer twin of `GLOBAL-DQ-021`, which reads the event layer and cannot see a Comp.Rank. The layer forces two differences, and both are what keep it usable. An event enters a team as one participant while a Comp.Rank lists it athlete by athlete, so a place is legitimately held by as many participants as a team fields, read from the statistic itself rather than assumed - without that every relay is reported. And a Comp.Rank ranks a whole competition rather than one contest, so it stops ranking individually where the play-offs begin and enters everyone below as one joint place; a place shared by k entries consumes the k-1 places under it, so a well-formed sharing leaves them empty while a place stored inside that span is a place occupied twice - the same rule `GLOBAL-DQ-026` applies to a duplicated medal. Without that second condition the check reports the convention rather than the defect, at roughly thirty times the volume. Every subquery carries the sport filter rather than inheriting it from an outer join, because the participant and data shards are shared by every sport | WRONG_RESULTS |
| GLOBAL-DQ-096 | EVENT_NAME_DOES_NOT_NAME_ITS_PARTICIPANTS | `HIERARCHY.sql` | Finds events whose name is built from their competitors but does not name one of them, separating naming none of them from naming only some. | `SPORT_ID` | Sport confirmed to name its events after the competitors that play them. A sport whose event names are codes, distances or round labels must not instantiate this: it names no participant and its whole population is reported. Tested by containment rather than by rebuilding the name, so the separator convention is not a parameter and a side spelled as two joined country names still matches. A participant with an empty name is excluded rather than reported, because there is nothing to look for; that gap is `GLOBAL-DQ-008`. Asserts what `GLOBAL-DQ-049` cannot: that check reads a name for text hygiene, never against the objects the name is made of | MALFORMED_NAME |
| GLOBAL-DQ-097 | EVENT_ROUND_TYPE_KNOCKOUT_FLAG_CONTRADICTS_ROUND | `HIERARCHY.sql` | Finds round types whose knockout flag contradicts the round: an elimination round marked not knockout, or a round nobody leaves marked as one. | `SPORT_ID`, `ELIMINATION_ROUND_NAME_LIST`, `GROUP_ROUND_NAME_LIST` | Any sport contesting rounds whose nature is readable from their name. `round_type` stores a name, a bracket value and the flag, and nothing that says whether entries go out, so the two lists are the judgement the check rests on and a name in neither is left unjudged rather than assumed. Reported per round type because that is where the defect lives and where it is repaired; the event count travels with the row so the exposure stays visible. `DB-SEM-012` records that one round name exists under both a knockout and a non-knockout id, so a sport storing the wrong member of a pair reports every event using it - a high count here is the size of one misconfiguration, not a list of separate ones | WRONG_STRUCTURE |
| GLOBAL-DQ-098 | COMP.RANK_TEAM_FIELD_UNUSED_IN_TEAM_STATISTIC | `STATISTICS.sql` | Finds Comp.Rank where every athlete shares a place with the same number of others and none carries a Team value, so teams of that size are scored without recording which team each athlete belongs to. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_TEAM_TYPE_ID` | Tournament-owned statistics only; sport confirmed to record an athlete's team in the Team data field. Restricted to athlete participants, because a statistic ranking teams shares a place by the joint-place convention instead and a team belongs to no team. What it asserts is deliberately narrow - every athlete in a group and every group the same size, which is a relay or team format and nothing else - because sharing that covers part of the field, or covers it in ragged groups, is the joint placing an elimination round produces and reporting it would name a convention rather than a missing link. A sport contesting no team or relay format inside Comp.Rank reports nothing by construction, which is the check standing guard rather than a misdirected scope - the coverage count still shows the statistics it read. Invisible to `GLOBAL-DQ-064`, whose eligible population is the statistic that uses the field at all, so a statistic using it for nobody is excluded there rather than reported. Names the cause behind `GLOBAL-DQ-095`, which reads how many participants a place may hold from this same field and can only assume one where it is unused | MISSING_VALUES |
| GLOBAL-DQ-099 | COMP.RANK_VALUE_BELONGS_TO_ANOTHER_FIELD | `STATISTICS.sql` | Finds a Comp.Rank value that is exactly what another field owns: a medal word stored outside the Medal field, or a plain place number stored inside it. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID` | Tournament-owned statistics only; sport restricting its Medal field to gold, silver and bronze. Every other value check asks whether a value fits the field holding it - `GLOBAL-DQ-012` for Rank, `-027` for Medal, `-057` for Comment, `-077` for a numeric field - and none asks whose value it is, so a medal word in a rank field is reported there as "not a rank" and the reader is left to notice it is a good medal one column away. That is the difference between correcting a value and moving it. Only exact matches count, on the closed vocabulary the medal checks already assert everywhere, so a comment merely containing the word is left alone | WRONG_RESULTS |
| GLOBAL-DQ-100 | COMP.RANK_DISCIPLINE_NOT_CONTESTED_IN_TOURNAMENT | `STATISTICS.sql` | Finds Comp.Rank claiming a discipline that no event under its own tournament was contested in. | `SPORT_ID`, `STATISTIC_TYPE_ID` | Sport carrying disciplines on both statistics (`object_discipline` owner 83) and events (owner 5). Measured against the tournament's own events rather than against the statistic's name, which is what makes it global - a name-to-discipline map has to be written per sport and is the parameter this file warns turns one template into a configuration exercise. A tournament running several disciplines is not reported for the difference between them: the claim only has to appear somewhere among the events. A tournament whose events carry no discipline is outside the eligible population rather than a finding, because there is nothing to measure against; that gap is `GLOBAL-DQ-015` | WRONG_DISCIPLINE |

| GLOBAL-DQ-101 | COMP.RANK_SETTINGS_EVENT_ID_INVALID_OR_OUTSIDE_TOURNAMENT | `STATISTICS.sql` | Finds Comp.Rank whose Event id config does not resolve to an event under its own tournament, separating a value that is not a number, one naming no event, and one naming an event another tournament owns. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_EVENT_ID_TYPE_ID` | Sport whose statistic_config records an Event id. Curling declares CONFIG_EVENT_ID_TYPE_ID not applicable, so it is out of scope there. Assert the id itself rather than trusting the joins that walk it, which drop what they cannot match. | NO_RELATED_RECORDS |
| GLOBAL-DQ-102 | EVENT_SCOPE_RESULT_OWNER_EVENT_MISMATCH | `RESULTS.sql` | Finds scope results naming an event participant from a different event, or one that is not active, so the value is attached to a competitor who did not play it. | `SPORT_ID`, `SCOPE_TYPE_LIST` | Sport writing an active event_scope layer. A pure relational invariant, so it needs no vocabulary parameter and no format judgement; a sport with no scope row has an empty scope rather than a clean one. | WRONG_STRUCTURE |
| GLOBAL-DQ-103 | COMP.RANK_PARTICIPANT_DUPLICATE_IN_STATISTIC | `STATISTICS.sql` | Finds Comp.Rank holding the same participant twice, so one competitor occupies a place in the ranking two times over. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Any sport with Comp.Rank statistics. Distinct from GLOBAL-DQ-060, which groups by participant row and data type and therefore cannot see one participant holding two participant rows. | WRONG_STRUCTURE |

| GLOBAL-DQ-104 | EVENT_PARTICIPANT_REFERENCE_OR_TYPE_INVALID | `PARTICIPANTS.sql` | Finds events holding a participant row that resolves to nothing, or to a participant of a type the sport does not field, separating the two. | `SPORT_ID`, `EVENT_PARTICIPANT_TYPE_LIST` | Any sport with a confirmed EVENT_PARTICIPANT_TYPE_LIST. That list is a scope filter everywhere else, so this is the only statement that tests it rather than trusting it; filtering on a value never checks it. | WRONG_STRUCTURE |
| GLOBAL-DQ-105 | COMP.RANK_SETTINGS_SCALAR_DUPLICATE_ROWS | `STATISTICS.sql` | Finds Comp.Rank holding more than one config row for a setting that takes one value - start date, end date or gender - separating a repeat of the same value from two that contradict each other. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_START_DATE_TYPE_ID`, `CONFIG_END_DATE_TYPE_ID`, `CONFIG_GENDER_TYPE_ID` | Any sport recording the three singleton Comp.Rank settings. The Event id config is out of scope by design, being legitimately multi-valued - whether its values resolve is GLOBAL-DQ-101. | WRONG_STRUCTURE |

| GLOBAL-DQ-106 | COMP.RANK_UNEXPECTED_OWNER_TYPE | `STATISTICS.sql` | Finds Comp.Rank hanging off an owner level other than the one the sport is confirmed to use. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `STATISTIC_OWNER_TYPE_ID` | Any sport with a confirmed STATISTIC_OWNER_TYPE_ID. It is the one Comp.Rank statement that does not anchor on object_typeFK = 3, because a statistic on the wrong level is invisible to every statement that joins through the level it should be on. | WRONG_STRUCTURE |
| GLOBAL-DQ-107 | EVENT_SCOPE_CONTAINER_MISSING_FOR_FINISHED | `RESULTS.sql` | Finds finished events holding no scope container, so a match that was played records no period-by-period breakdown. | `SPORT_ID`, `SCOPE_TYPE_ID` | Sport writing an active event_scope layer. Only the missing container is asserted; more than one per event occurs in no confirmed sport, and a rule with no observed population cannot be tested. | NO_RELATED_RECORDS |
| GLOBAL-DQ-108 | EVENT_RESULTS_SCORE_NEGATIVE_OR_FRACTIONAL | `RESULTS.sql` | Finds deciding or mirrored score values that are negative or carry a fractional part, so a count of scoring units is stored as something no count can be. | `SPORT_ID`, `RESULT_FINAL_SCORE_TYPE_ID`, `RESULT_MIRROR_SCORE_TYPE_ID` | Sport whose deciding score is a count of scoring units. Tighter than GLOBAL-DQ-076, which asks only whether the value is numeric and therefore accepts a negative or fractional score. | WRONG_RESULTS |

| GLOBAL-DQ-109 | EVENT_SETTINGS_DISCIPLINE_STORAGE_MISMATCH | `HIERARCHY.sql` | Finds events whose discipline differs between the two storage paths, the discipline event property and the object_discipline relation. | `SPORT_ID` | Sport storing the discipline both as an event property and as an object_discipline relation. Only events carrying an active value that resolves to an active discipline through both paths are eligible; a single record is not a disagreement, and the two paths are reduced to distinct sets before comparison. | WRONG_DISCIPLINE |
| GLOBAL-DQ-110 | COMP.RANK_DISCIPLINE_CONTRADICTS_LINKED_EVENT | `STATISTICS.sql` | Finds Comp.Rank whose claimed discipline was contested by none of the events it names through the Event id config, so the two paths disagree about the same competition. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_EVENT_ID_TYPE_ID` | Sport recording an Event id on its Comp.Rank statistics. Narrower than GLOBAL-DQ-100, which can only measure the claim against everything the tournament contested; where the ids exist the statistic names its own events. | WRONG_DISCIPLINE |
| GLOBAL-DQ-111 | EVENT_RESULTS_RANK_EFFECTIVE_TIME_NOT_MONOTONIC | `RESULTS.sql` | Finds events in the sport's timed disciplines where a finisher placed behind another records a faster time, or whose effective time cannot be read at all - the Full time where stored, the Duration otherwise - ignoring participants a Comment marks as not finishing. | `SPORT_ID`, `TIMED_DISCIPLINE_LIST`, `RESULT_RANK_TYPE_ID`, `RESULT_DURATION_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `RESULT_COMMENT_TYPE_ID`, `RESULT_COMMENT_NO_RESULT_LIST` | Sport whose Duration column follows the leader/gap convention, the leader carrying an absolute time and everyone behind a gap marked by a leading plus. The coverage population excludes confirmed non-finishers exactly as the findings population does. Every remaining effective time is eligible for the format audit; only readable values of the same kind and with different ranks enter the monotonic comparison, and an unreadable value is reported even when it has no comparable peer. Confirm the convention in the sport file before instantiating. | WRONG_RESULTS |

| GLOBAL-DQ-112 | EVENT_LINEUP_ATHLETE_ASSIGNED_MORE_THAN_ONCE | `PARTICIPANTS.sql` | Finds events where one athlete occupies two places at once, repeated inside a single team's lineup or held by two teams. | `SPORT_ID` | Sport using lineups as its membership mechanism. Where lineups are the minority mechanism, as in Curling, the coverage count is the number to read first: a small eligible population makes any finding count meaningless on its own. | WRONG_STRUCTURE |
| GLOBAL-DQ-113 | COMP.RANK_PARTICIPANT_TYPE_MIXED | `STATISTICS.sql` | Finds Comp.Rank holding participants of more than one kind, so one place sequence ranks teams against individuals. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Any sport with Comp.Rank statistics. It names no legitimate kinds, which is what keeps it global - the sport vocabulary is GLOBAL-DQ-104, and a sport fielding both teams and individuals is normal; one statistic holding both is not. | WRONG_STRUCTURE |

| GLOBAL-DQ-114 | EVENT_RESULTS_MIRROR_SCORE_WITHOUT_DECIDING_SCORE | `RESULTS.sql` | Finds finished events holding a running or mirrored score while no participant holds the deciding score, so the event was scored but never resolved. | `SPORT_ID`, `RESULT_FINAL_SCORE_TYPE_ID`, `RESULT_MIRROR_SCORE_TYPE_ID` | Sport storing both a deciding score and a mirror of it. Distinct from GLOBAL-DQ-017, which reports a finished event with no result at all and therefore never sees an event that holds one kind of score but not the other. | WRONG_RESULTS |

| GLOBAL-DQ-115 | COMP.RANK_PARTICIPANT_REFERENCE_INVALID | `STATISTICS.sql` | Finds Comp.Rank participants whose reference resolves to no participant row, or to a soft-deleted one, separating the two, with the data rows still attached to them. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Any sport with a confirmed Comp.Rank participant shard. The participant relation is the assertion rather than a scope join: findings use a left join, while coverage deliberately counts every active statistic-participant row before reference resolution so dangling references cannot disappear as false-clean scope. | WRONG_STRUCTURE |
| GLOBAL-DQ-116 | EVENT_RESULTS_RANK_TIE_CONTRADICTED_BY_SCORE | `RESULTS.sql` | Finds event participants sharing a Rank with another in a finished event while the score that decides the ranking does not agree across the tie, separating a tie whose scores differ from one where a tied participant holds no score at all. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_COMMENT_TYPE_ID`, `RESULT_SCORE_TYPE_ID` | Sport that decides its within-event ranking from one scalar score stored as a result, recorded in `RESULT_SCORE_TYPE_ID`. A sport ranked by a time, by a head-to-head outcome or by nothing stored must not instantiate this. It asks a different question from `GLOBAL-DQ-021`, which reports a shared rank unless a Comment explains it and therefore cannot be used by a sport whose scoring ties by design: a segment scored in points produces genuine ties in quantity, and only the score itself says which of them is real. Where both apply the two overlap, so a sport instantiating this one should say why it also needs the Comment rule. A tie whose scores are simply absent is separated rather than merged, because an unverifiable tie and a contradicted one are repaired differently. Scores are compared as quantities where they are numeric, so a sport writing `13.800` beside `13.8` is not reported for a difference in trailing zeros; a value that is not a plain number keeps its own text identity, which a sport storing a status word in its score field relies on. The eligible population is every event whose `status_type` is `finished`, not only the one carrying the plain finished detail | WRONG_RESULTS |
| GLOBAL-DQ-117 | EVENT_RESULTS_COMMENT_INVALID_OR_CONTRADICTED_BY_SCORE | `RESULTS.sql` | Finds Comment values outside the sport's status codes, or marking a participant as unclassified while a Rank, a Medal or a score is stored for that same participant. | `SPORT_ID`, `RESULT_COMMENT_TYPE_ID`, `RESULT_COMMENT_VALUE_LIST`, `RESULT_COMMENT_NO_RESULT_LIST`, `RESULT_RANK_TYPE_ID`, `RESULT_MEDAL_TYPE_ID`, `RESULT_SCORE_TYPE_ID` | Sport whose Comment result resolves to a closed set of status codes, and which settles a placing from a stored scalar score rather than from a time. **A sport instantiates this or `GLOBAL-DQ-052`, never both.** The two read the same population and both emit `COMMENT_INVALID_VALUE`, so a sport carrying rows for both reports every unrecognised status twice under different CheckIDs. The choice is not a preference: `GLOBAL-DQ-052` tests the contradiction against a full time and a duration, so a sport storing neither can never run it, and this one puts the score where those were. A sport storing a time on its result keeps `GLOBAL-DQ-052`, whose time arm is the stronger test there | WRONG_RESULTS |

<!-- MANUAL PASTE ZONE: GLOBAL DQ REGISTRY — insert approved additions immediately before this marker; do not move or delete it. -->

## Qualification rule

A check qualifies as a GLOBAL DQ template only when:

- its relation path is supported by `DATABASE.md`;
- the violation it asserts is wrong in every applicable sport, not only in the sport it was
  first written for;
- every sport-varying input is a declared parameter;
- it contains no hard-coded sport ID or sport name;
- its applicability prerequisite is stated, so a sport where the condition is legitimately
  absent is excluded rather than reported as violating;
- one CheckID identifies exactly one executable statement.

The last point matters most in practice. "Every stage has an age class" is true for some
sports and meaningless for others; such a check is global only because the prerequisite
column says where it applies.

**"Legitimately absent" means structurally absent, and is never read off a row count.** The
prerequisite column describes what a sport *stores* — which columns, layers and relations its
records use — not how many rows currently carry a given value. A sport whose events hold a
status column satisfies the prerequisite of a status rule even while no event carries that
status today; excluding it on that count silently disables the rule for the day the first
such row arrives, which is the day it was written for. The same applies to a value list, a
type or a discipline that happens to be unused this month. Where the two are hard to tell
apart, ask before classifying. `TOOLS/README.md` owns the same distinction for `_checkSignal`
and `WORKFLOW.md` states it for scope.

## Promoting a sport check to a template

Only after explicit user approval:

1. prove the violation condition is not sport-specific;
2. replace every sport literal with a declared parameter;
3. state the applicability prerequisite;
4. assign the next unused `GLOBAL-DQ-NNN`;
5. insert the statement in its domain file by CheckID order;
6. insert the registry row immediately before the marker above;
7. set the originating sport's registry `Family` to the new template ID;
8. run `TOOLS/Test-Package.ps1`.

A sport check keeps its own CheckID after promotion. `Family` records that the two are the
same logical check; the sport CheckID is never renumbered or reused.

## When not to make a check global

A template that fits every sport is worth having. A template stretched until it fits every
sport is not, and the difference is visible in the parameter list: each new flag added to
keep one more sport inside a shared statement is a place where the next reader has to work
out which sport the branch was for.

**Several narrower checks that work beat one wide check that has to be configured into
correctness.** A sport whose competition model genuinely differs is better served by its own
statement than by a parameter that switches the shared one's meaning.

The clearest line is the competition model `DATABASE.md` `DB-SEM-015` records:

- in a **head-to-head team sport** the same two sides do not meet twice inside a few hours,
  every event holds exactly two entries, and a result is read as a pair;
- in a **listing sport** the same field is entered several times in a day by design - motos,
  heats, runs - every event holds many entries, and a result is read against the field.

An assertion that is obviously true in one of those is often meaningless or actively wrong in
the other, and no parameter repairs that: it only hides which case the reader is in.
`GLOBAL-DQ-062` is the worked example. Whether two events sharing a day and a participant set
are a duplicate has opposite answers in the two models, and it needed two declared facts -
`DUPLICATE_KEY_USES_CALENDAR_DAY` and `DUPLICATE_KEY_INCLUDES_EVENT_NAME` - to stay one
statement. That was still worth it there, because the two facts are small and orthogonal and
the finding is the same defect either way. A third flag would not have been.

Two consequences for practice:

- when a template needs a new parameter only so that one sport stops being reported, ask
  first whether that sport should have its own statement instead;
- **a change to a shared template is measured against every sport that instantiates it,
  before it is kept.** Running it only for the sport in hand is how a silent regression
  reaches the sports nobody was looking at.

<!-- MANUAL PASTE ZONE: GLOBAL DQ MODEL BOUNDARY — insert approved additions immediately before this marker; do not move or delete it. -->

## Manual registry-rule additions

Registry-policy changes are inserted immediately before the marker below.

<!-- MANUAL PASTE ZONE: GLOBAL DQ RULES — insert approved additions immediately before this marker; do not move or delete it. -->
