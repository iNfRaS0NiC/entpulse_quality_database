# GLOBAL DQ Template Registry

## Purpose

A GLOBAL DQ template is one approved data-quality check whose logic holds for every
applicable sport, stored once and executed for a selected sport by substituting its
declared parameters. It carries the full `POWERBI.md` contract — identity header,
findings-plus-coverage, audited-object granularity, commented scope filter — and differs
from a sport check only in that its sport-specific values are parameters instead of
literals.

This exists because the alternative does not scale. A hand-copied sport file is ~60 lines
per check; at 110 sports one logic fix would have to be applied 110 times, with nothing
detecting the copies that drifted. A template is fixed once and every sport that
instantiates it follows.

`GLOBAL_QUERIES/` answers *what is stored*. `GLOBAL_DQ/` asserts *what is wrong*. The
boundary between discovery and DQ is unchanged and owned by `POWERBI.md`.

## What is still not automatic

A template is not permission to check a sport. The authorization gate in `POWERBI.md`
applies unchanged: DQ work starts only after the user names a sport and a category, and a
check is instantiated for that sport only after the user approves it.

What changes is what approval produces. Instead of a new ~60-line statement, it produces:

1. one row in `POWERBI_REGISTRY.md` whose `Family` column names the template;
2. the parameter values for that sport in `SPORTS/params.json`, if not already recorded.

No per-sport copy of the SQL is created. A check that genuinely cannot be expressed
through declared parameters stays sport-authored, with `Family` = `—` and its statement in
`POWERBI_QUERIES/<SportSlug>.sql`.

## Parameter contract

Mandatory parameters use the same uppercase double-brace tokens as `GLOBAL_QUERIES/`, and
`WORKFLOW.md` owns the general rule. Two kinds occur:

| Kind | Substitution | Example |
|---|---|---|
| Scalar | A numeric or quoted value in a comparison | `{{SPORT_ID}}`, `{{STATISTIC_TYPE_ID}}` |
| Physical name fragment | Text inside a table or column name | `statistic_participants{{SHARD_ID}}` |
| List | A quoted, comma-separated list inside `IN (...)` | `{{EVENT_PARTICIPANT_TYPE_LIST}}` |

A list parameter's value already contains its quotes: `'athlete'` or `'athlete', 'team'`.
It is a SQL fragment, so it is only ever filled from `SPORTS/params.json`, never from
user-supplied text.

| Parameter | Meaning |
|---|---|
| `{{SPORT_ID}}` | Confirmed numeric `sport.id` |
| `{{EVENT_PARTICIPANT_TYPE_LIST}}` | Participant types the sport uses in `event_participants` |
| `{{REGISTRY_PARTICIPANT_TYPE_LIST}}` | Participant types present in the sport's `object_participants` registry |
| `{{STATISTIC_TYPE_ID}}` | Confirmed `statistic.statistic_typeFK` |
| `{{SHARD_ID}}` | Confirmed physical statistic participant/data shard number |
| `{{DATA_RANK_TYPE_ID}}` | `statistic_data_type.id` of the Rank data field for that statistic type |
| `{{CONFIG_START_DATE_TYPE_ID}}` | `statistic_data_type.id` of the Start date config field |
| `{{CONFIG_END_DATE_TYPE_ID}}` | `statistic_data_type.id` of the End date config field |
| `{{CONFIG_EVENT_ID_TYPE_ID}}` | `statistic_data_type.id` of the Event id config field |
| `{{DATA_COMMENT_TYPE_ID}}` | `statistic_data_type.id` of the Comment data field |
| `{{DATA_MEDAL_TYPE_ID}}` | `statistic_data_type.id` of the Medal data field |
| `{{DATA_TIME_TYPE_ID}}` | `statistic_data_type.id` of the current Time data field |
| `{{DATA_TIME_DIFFERENCE_TYPE_ID}}` | `statistic_data_type.id` of the Time Difference data field |
| `{{DATA_DEPRECATED_DURATION_TYPE_ID}}` | `statistic_data_type.id` of the superseded Duration data field |
| `{{RESULT_RANK_TYPE_ID}}` | `result_type.id` of the sport's Rank event result |
| `{{RESULT_COMMENT_TYPE_ID}}` | `result_type.id` of the sport's Comment event result |
| `{{RESULT_MEDAL_TYPE_ID}}` | `result_type.id` of the sport's Medal event result |
| `{{RESULT_DURATION_TYPE_ID}}` | `result_type.id` of the sport's leader/gap Duration event result |
| `{{CONFIG_GENDER_TYPE_ID}}` | `statistic_data_type.id` of the Gender config field |
| `{{FINAL_ROUND_TYPE_LIST}}` | Comma-separated `round_type.id` values the sport treats as its decisive Final round |
| `{{RANK_MAX_PLAUSIBLE}}` | Highest rank value the sport can legitimately award in one event |
| `{{NOT_STARTED_DESC_LIST}}` | Comma-separated `status_descFK` values under `status_type='notstarted'` that the sport expects to carry no results |
| `{{TIMED_DISCIPLINE_LIST}}` | Comma-separated `discipline.id` values in which the sport records a measured time; judged disciplines are excluded |
| `{{RESULT_FULL_TIME_TYPE_ID}}` | `result_type.id` of the sport's absolute full-time event result |
| `{{STALE_NOT_STARTED_DAYS}}` | Days after its start date beyond which the sport treats a still not-started event as stale |
| `{{PLACEHOLDER_COUNTRY_LIST}}` | A quoted, comma-separated list of `country.name` values the sport treats as a placeholder rather than a real country |
| `{{DATA_TEAM_TYPE_ID}}` | `statistic_data_type.id` of the Team data field, which holds the `participant.id` of the team an athlete competes for inside the statistic |

Every value is a confirmed structural fact recorded in the sport's file before it reaches
`SPORTS/params.json`. A parameter that is not yet confirmed for a sport means the template
cannot be instantiated for it yet — not that a plausible value may be guessed.

## Sport parameter file

`SPORTS/params.json` maps a sport slug to its confirmed values:

```json
{
  "BMX": {
    "SPORT_ID": 58,
    "EVENT_PARTICIPANT_TYPE_LIST": "'athlete'",
    "REGISTRY_PARTICIPANT_TYPE_LIST": "'athlete', 'team'",
    "STATISTIC_TYPE_ID": 11,
    "SHARD_ID": 11
  }
}
```

`TOOLS/Run-Query.ps1 -Sport <name>` reads it before querying the database, so a recorded
value is preferred over a discovered one: the file holds evidence, discovery holds a
heuristic. An explicit `-SportId` or `-Params` still overrides both.

The file is data, not documentation. The narrative evidence for each value stays in
`SPORTS/<SportSlug>.md`; `TOOLS/Test-Package.ps1` checks that the two agree on `SPORT_ID`.

## Registry

The `Description` column mirrors each statement's `-- What it does:` comment.

| CheckID | Name | File | Description | Mandatory parameters | Applicability/prerequisite | Category |
|---|---|---|---|---|---|---|
| GLOBAL-DQ-001 | TEMPLATE_NO_TOURNAMENTS_OR_STAGES | `HIERARCHY.sql` | Finds active tournament templates, excluding IOC-purpose templates, with zero active tournaments, or with tournaments but zero active tournament stages across all of them, together with a coverage count of all eligible templates. | `SPORT_ID` | Any sport with active templates | NO_RELATED_RECORDS |
| GLOBAL-DQ-002 | TOURNAMENT_STAGE_MISSING_AGE_CLASS | `HIERARCHY.sql` | Finds active tournament stages without an active tournament_age_class relation via object_relation, together with a coverage count of all eligible stages. | `SPORT_ID` | Sport whose stages are expected to carry an age class (`object_relation 4 -> 151`) | MISSING_VALUES |
| GLOBAL-DQ-003 | TOURNAMENT_STAGE_NO_EVENTS | `HIERARCHY.sql` | Finds active tournament stages with zero active events, together with a coverage count of all eligible stages. | `SPORT_ID` | Any sport with active stages | NO_RELATED_RECORDS |
| GLOBAL-DQ-004 | TOURNAMENT_STAGE_DATE_RANGE_MISMATCH | `HIERARCHY.sql` | Finds active tournament stages whose start or end date does not match the earliest and latest active event start date within the stage, together with a coverage count of all eligible stages with at least one event. | `SPORT_ID` | Sport whose stage date range is expected to bound its events | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-005 | TOURNAMENT_STAGE_MISSING_START_OR_END_DATE | `HIERARCHY.sql` | Finds active tournament stages with a NULL start date or end date, together with a coverage count of all eligible stages. | `SPORT_ID` | Any sport with active stages | MISSING_VALUES |
| GLOBAL-DQ-006 | EVENT_MISSING_ROUND_TYPE | `HIERARCHY.sql` | Finds active events with a NULL round_typeFK or a round_typeFK not matching any round_type row, with template, tournament and stage name context, together with a coverage count of all eligible events. | `SPORT_ID` | Sport confirmed to use `event.round_typeFK` | MISSING_VALUES |
| GLOBAL-DQ-007 | PARTICIPANT_MISSING_DATE_OF_BIRTH | `PARTICIPANTS.sql` | Finds active participants of the selected types that take part in at least one active event of the sport but have no active, non-empty date_of_birth property value, with their participation count and the count of participations carrying at least one active result, together with a coverage count of all eligible participants. | `SPORT_ID`, `EVENT_PARTICIPANT_TYPE_LIST` | Sport whose participants are expected to carry a `date_of_birth` property | MISSING_VALUES |
| GLOBAL-DQ-008 | PARTICIPANT_MISSING_PROFILE_FIELDS | `PARTICIPANTS.sql` | Finds active participants of the selected types that take part in at least one active event of the sport and are missing name, country, first_name and/or last_name, together with a coverage count of all eligible participants. | `SPORT_ID`, `EVENT_PARTICIPANT_TYPE_LIST` | Sport confirmed to store split first/last name via `language` types 7 and 8 | MISSING_VALUES |
| GLOBAL-DQ-009 | PARTICIPANT_NO_EVENT_PARTICIPATION | `PARTICIPANTS.sql` | Finds active participants of the selected types linked through active sport-registry rows but having zero active event_participants rows within the sport, together with a coverage count of the same registered population. | `SPORT_ID`, `REGISTRY_PARTICIPANT_TYPE_LIST` | Sport with an active `object_participants` registry (`object='sport'`) | NO_RELATED_RECORDS |
| GLOBAL-DQ-010 | COMP.RANK_NO_PARTICIPANTS | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type with zero active participant rows in the confirmed physical shard, with template and tournament name context, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Tournament-owned statistics only (`statistic.object_typeFK=3`); shard confirmed per `DB-SEM-006` | NO_RELATED_RECORDS |
| GLOBAL-DQ-011 | COMP.RANK_SETTINGS_MISSING_START_OR_END_DATE | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type with a missing or empty Start date or End date config value, with template and tournament name context, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_START_DATE_TYPE_ID`, `CONFIG_END_DATE_TYPE_ID` | Tournament-owned statistics only; both config field IDs confirmed for the statistic type | MISSING_VALUES |
| GLOBAL-DQ-012 | COMP.RANK_RESULTS_RANK_INVALID_OR_MISSING | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type holding at least one participant whose Rank data value is missing, empty, non-numeric or not a positive integer, with template and tournament name context and the count of affected participant rows, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID` | Tournament-owned statistics only; sport confirmed to store a positive-integer Rank data field | WRONG_RESULTS |
| GLOBAL-DQ-013 | TEMPLATE_MISSING_SET_SUBSET_GENDER_NAME | `HIERARCHY.sql` | Finds active tournament templates, excluding IOC-purpose templates, missing name, gender, an active subset relation, or a resolvable tournament set, together with a coverage count of all eligible templates. | `SPORT_ID` | Sport whose templates are expected to sit in a tournament set through `object_relation 2 -> 152` | MISSING_VALUES |
| GLOBAL-DQ-014 | TEMPLATE_STAGE_GENDER_MISMATCH | `HIERARCHY.sql` | Finds active tournament stages whose gender differs from their parent template gender, excluding cases where either side is mixed or empty, together with a coverage count of all eligible stages. | `SPORT_ID` | Sport where template and stage are expected to agree on gender; mixed is excluded on both sides | WRONG_GENDER |
| GLOBAL-DQ-015 | EVENT_SETTINGS_MISSING_DISCIPLINE | `HIERARCHY.sql` | Finds active events without an active object_discipline relation (owner type=5), with template, tournament and stage name context, together with a coverage count of all eligible events. | `SPORT_ID` | Sport confirmed to carry disciplines on events (`object_discipline` owner type 5) | MISSING_VALUES |
| GLOBAL-DQ-016 | EVENT_SETTINGS_MISSING_GENDER | `HIERARCHY.sql` | Finds active events whose parent tournament stage has a NULL, empty or undefined gender value, with template, tournament and stage name context, together with a coverage count of all eligible events. | `SPORT_ID` | Sport where the event's gender is carried by its parent stage | MISSING_VALUES |
| GLOBAL-DQ-017 | EVENT_RESULTS_MISSING_FOR_FINISHED | `RESULTS.sql` | Finds active events with status_type='finished' (status_descFK=6) where none of their event participants has an active, non-empty result row, with template, tournament, stage name and status context, together with a coverage count of all eligible finished events. | `SPORT_ID` | Sport confirmed to use `finished`/6 and to store results on `event_participants` | WRONG_RESULTS |
| GLOBAL-DQ-018 | EVENT_RESULTS_MEDAL_INVALID_VALUE | `RESULTS.sql` | Finds active event-participant rows with an active, non-empty Medal result value that is not gold, silver or bronze, together with a coverage count of all eligible event-participants with an active, non-empty Medal value. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID` | Sport confirmed to store a Medal event result restricted to gold/silver/bronze | WRONG_RESULTS |
| GLOBAL-DQ-019 | EVENT_DURATION_FORMAT_MISMATCH_TO_RANK | `RESULTS.sql` | Finds active events containing at least one participant whose duration result does not follow the leader/gap convention for their rank result (rank 1 plain absolute time, every other rank plus-prefixed gap), with the count, type and per-participant detail per event, together with a coverage count of all eligible events. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_DURATION_TYPE_ID` | Sport confirmed to follow the leader/gap convention on its duration result type; the accepted time shape is colon-tolerant, so a sport with a stricter shape needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-020 | EVENT_RESULTS_RANK_OUTLIER_ABOVE_FIELD_SIZE | `RESULTS.sql` | Finds active event-participant rows in finished events whose numeric Rank exceeds the event's active participant count and is disconnected from the next lower Rank in the same event, while carrying no active Comment value, together with a coverage count of all eligible event-participants holding an active numeric Rank in a finished event. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_COMMENT_TYPE_ID` | Sport confirmed to mark non-finishing participants through the Comment result rather than through a rank sentinel alone | WRONG_RESULTS |
| GLOBAL-DQ-021 | EVENT_RESULTS_RANK_DUPLICATE_WITHOUT_COMMENT | `RESULTS.sql` | Finds active event-participant rows in finished events sharing one numeric Rank with another participant of the same event where neither row carries an active Comment value, with the count of participants sharing that rank unexplained, together with a coverage count of all eligible event-participants holding an active numeric Rank in a finished event. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_COMMENT_TYPE_ID` | Sport where a shared rank is legitimate only when explained by a Comment value; excludes sports that award tied ranks by design | WRONG_RESULTS |
| GLOBAL-DQ-022 | COMP.RANK_SETTINGS_MISSING_AGE_CLASS | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type without an active tournament_age_class relation via object_relation (owner type=83), with template and tournament name context, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID` | Tournament-owned statistics only; sport confirmed to carry `object_relation 83 -> 151` | MISSING_VALUES |
| GLOBAL-DQ-023 | COMP.RANK_SETTINGS_MISSING_DISCIPLINE | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type with zero active object_discipline relations (owner type=83), or with a relation whose disciplineFK does not resolve to an active discipline, with template and tournament name context, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID` | Tournament-owned statistics only; sport confirmed to carry disciplines on statistics (`object_discipline` owner type 83) | MISSING_VALUES |
| GLOBAL-DQ-024 | COMP.RANK_SETTINGS_DATE_RANGE_MISMATCH_STAGE | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type whose Start date and End date config values do not match the earliest and latest active tournament_stage start and end date under the statistic's tournament, together with a coverage count of all eligible statistics carrying at least one active config date. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_START_DATE_TYPE_ID`, `CONFIG_END_DATE_TYPE_ID` | Tournament-owned statistics only; sport whose statistic date range is expected to bound its tournament's stages | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-025 | COMP.RANK_SETTINGS_DATE_RANGE_MISMATCH_EVENTS | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type whose Start date and End date config values do not match the earliest and latest startdate of the events referenced through the Event id config field, together with a coverage count of all eligible statistics with at least one linked event and at least one active config date. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_START_DATE_TYPE_ID`, `CONFIG_END_DATE_TYPE_ID`, `CONFIG_EVENT_ID_TYPE_ID` | Tournament-owned statistics only; sport confirmed to populate the Event id config field, which is optional today | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-026 | COMP.RANK_SETTINGS_MISSING_MEDAL | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type where at least one of the gold, silver or bronze Medal values is absent among statistic participants, distinguishing statistics with no medals at all from those missing only specific medal types, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID` | Tournament-owned statistics only; sport whose statistics of this type are expected to award a full medal set | MISSING_VALUES |
| GLOBAL-DQ-027 | COMP.RANK_RESULTS_MEDAL_INVALID_VALUE | `STATISTICS.sql` | Finds active statistic-participant Medal rows of the selected statistic type, excluding IOC-purpose templates, whose value is present but not gold, silver or bronze, together with a coverage count of all eligible statistic-participant rows carrying any active, non-empty Medal value. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID` | Tournament-owned statistics only; sport confirmed to restrict the Medal data field to gold/silver/bronze | WRONG_RESULTS |
| GLOBAL-DQ-028 | COMP.RANK_RESULTS_TIME_DIFFERENCE_FORMAT_MISMATCH_TO_RANK | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type containing at least one participant whose Time Difference value does not follow the leader/gap convention for their Rank value, with the count, type and per-participant detail per statistic, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_TIME_DIFFERENCE_TYPE_ID` | Tournament-owned statistics only; sport confirmed to follow the leader/gap convention on the Time Difference data field | WRONG_RESULTS |
| GLOBAL-DQ-029 | COMP.RANK_RESULTS_DEPRECATED_DURATION_USED | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type where at least one participant still stores an active, non-empty value in the superseded Duration data field, reporting the count of participants using it and which current time fields are also populated, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_DEPRECATED_DURATION_TYPE_ID`, `DATA_TIME_TYPE_ID`, `DATA_TIME_DIFFERENCE_TYPE_ID` | Tournament-owned statistics only; sport confirmed to have superseded the Duration data field by the Time and Time Difference pair | WRONG_RESULTS |
| GLOBAL-DQ-030 | COMP.RANK_RESULTS_PARTICIPANT_NOT_IN_TOURNAMENT | `STATISTICS.sql` | Finds active statistic-participant rows of the selected statistic type whose participant takes part in no active event anywhere under the statistic's own tournament, with template and tournament name context, together with a coverage count of all eligible statistic-participant rows. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Tournament-owned statistics only; sport where a ranked participant is expected to have competed inside the same tournament | NO_RELATED_RECORDS |
| GLOBAL-DQ-031 | COMP.RANK_RESULTS_RANK_OUTLIER_ABOVE_FIELD_SIZE | `STATISTICS.sql` | Finds active statistic-participant rows of the selected statistic type whose numeric Rank exceeds the number of active participants in their own statistic and is disconnected from the next lower Rank, while carrying no active Comment value, together with a coverage count of all eligible statistic-participant rows holding an active numeric Rank. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_COMMENT_TYPE_ID` | Tournament-owned statistics only; sport confirmed to mark non-finishing ranked participants through the Comment data field | WRONG_RESULTS |
| GLOBAL-DQ-032 | COMP.RANK_RESULTS_NO_RANK_DATA_FOR_PARTICIPANTS | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type that hold at least one active participant but no active non-empty Rank data value for any of them, separating statistics holding no data at all from those holding other data but no Rank, together with a coverage count of all eligible statistics holding at least one active participant. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID` | Tournament-owned statistics only; sport confirmed to store a Rank data field for every ranked participant | MISSING_VALUES |
| GLOBAL-DQ-033 | COMP.RANK_RESULTS_MISSING_PHASE | `STATISTICS.sql` | Finds active statistic-participant rows of the selected statistic type carrying no active object_round phase row (object_typeFK=138, type='phase'), with template and tournament name context and the participant's Rank value, together with a coverage count of all eligible statistic-participant rows. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID` | Tournament-owned statistics only; sport confirmed to carry a Phase on ranked participants | MISSING_VALUES |
| GLOBAL-DQ-034 | TOURNAMENT_STAGE_MISSING_CORE_FIELDS | `HIERARCHY.sql` | Finds active tournament stages missing at least one of name, gender, country, host country, or an active city link, or carrying a country or host country that resolves only to a placeholder row and therefore reads as populated, together with a coverage count of all eligible stages. | `SPORT_ID`, `PLACEHOLDER_COUNTRY_LIST` | Any sport with active stages; all five fields are asserted, so a sport that legitimately leaves host country or city empty will report its whole population. A sport with no placeholder country row leaves `PLACEHOLDER_COUNTRY_LIST` naming values that simply never match | MISSING_VALUES |
| GLOBAL-DQ-035 | COMP.RANK_SETTINGS_MISSING_CORE_FIELDS | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type missing name, an active non-empty Gender config value, an active country relation (object_relation 83->33), or an active city relation (city_object owner type=83), or carrying a country relation that resolves only to a placeholder row and therefore reads as populated, with template and tournament name context, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_GENDER_TYPE_ID`, `PLACEHOLDER_COUNTRY_LIST` | Tournament-owned statistics only; all four fields are asserted, as for `GLOBAL-DQ-034` | MISSING_VALUES |
| GLOBAL-DQ-036 | EVENT_RESULTS_RANK_INVALID_OR_MISSING | `RESULTS.sql` | Finds active event-participant rows in finished events where the Rank value is not a strictly positive integer without leading zeros up to the sport's plausible maximum, or where Rank is missing and no active Comment exists either, separating participants holding no result of any type from those holding some other result, together with a coverage count of all eligible event-participants in finished events. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_COMMENT_TYPE_ID`, `RANK_MAX_PLAUSIBLE` | Sport whose largest legitimate field size is recorded in `RANK_MAX_PLAUSIBLE`; a mass-participation sport needs its own value | WRONG_RESULTS |
| GLOBAL-DQ-037 | EVENT_RESULTS_MISSING_MEDAL_FOR_FINAL | `RESULTS.sql` | Finds active, finished events on a Final round type where at least one of the gold, silver or bronze Medal result values is absent among event participants, distinguishing events with no medals at all from those missing specific medal types, together with a coverage count of all eligible Final-round finished events. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Sport whose Final-round events are expected to award a full medal set | MISSING_VALUES |
| GLOBAL-DQ-038 | EVENT_SETTINGS_MISSING_MEDAL_RELATED_FOR_FINAL | `HIERARCHY.sql` | Finds active, finished events on a Final round type that have no active event property named medal_related with value yes, with template, tournament and stage name context, together with a coverage count of all eligible Final-round finished events. | `SPORT_ID`, `FINAL_ROUND_TYPE_LIST` | Sport confirmed to carry the `medal_related` event property | MISSING_VALUES |
| GLOBAL-DQ-039 | EVENT_RESULTS_UNEXPECTED_MEDAL_FOR_NON_FINAL | `RESULTS.sql` | Finds active, finished events whose round type is not a Final that have at least one active, non-empty Medal result row for any event participant, with round-type context, together with a coverage count of all eligible non-Final finished events. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Sport that awards medals on the Final round only; excludes sports with medal placement rounds | WRONG_RESULTS |
| GLOBAL-DQ-040 | EVENT_FINAL_WITHOUT_COMP.RANK | `STATISTICS.sql` | Finds active events on a Final round type that no statistic of the selected type under their own tournament references through its Event id config field, separating tournaments holding no such statistic from those whose statistics declare no event scope, together with a coverage count of all eligible Final-round events. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `CONFIG_EVENT_ID_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Sport where every Final event is expected to have its own statistic, per `DB-SEM-013`; the Event id config field is optional today, so the two non-finding branches carry that fact | NO_RELATED_RECORDS |
| GLOBAL-DQ-041 | COMP.RANK_RESULTS_MEDAL_ON_NON_FINAL_PHASE | `STATISTICS.sql` | Finds active statistic-participant rows of the selected statistic type carrying an active non-empty Medal value while their object_round phase names a round type that is not a Final, with the offending phase, together with a coverage count of all eligible statistic-participant rows carrying an active non-empty Medal value. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_MEDAL_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Tournament-owned statistics only; sport confirmed to carry a Phase and to award medals on the Final round only | WRONG_RESULTS |
| GLOBAL-DQ-042 | EVENT_FINAL_PARTICIPANT_NOT_IN_COMP.RANK | `STATISTICS.sql` | Finds active event-participant rows in Final-round events whose participant appears in none of the populated statistics of the selected type that reference their event through the Event id config field, together with a coverage count of all eligible Final-round event-participants whose event is referenced by at least one populated statistic. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `CONFIG_EVENT_ID_TYPE_ID`, `FINAL_ROUND_TYPE_LIST` | Sport where a Final participant is expected to appear in the statistic covering that event; empty statistics are excluded so the finding is not a restatement of `GLOBAL-DQ-010` | NO_RELATED_RECORDS |
| GLOBAL-DQ-043 | EVENT_PARTICIPANTS_GENDER_MISMATCH | `PARTICIPANTS.sql` | Finds active event-participant rows whose gender contradicts the parent tournament stage or their own lineup: an athlete differing from a single-gender stage, a participant differing from the stage gender, a mixed team whose lineup lacks one gender, or a single-gender team whose lineup contains the other gender, together with a coverage count of all eligible event-participants in stages carrying a usable gender. | `SPORT_ID` | Sport whose stage gender is authoritative for its participants; the lineup branches apply only where a team entry has at least one active lineup row | WRONG_GENDER |
| GLOBAL-DQ-044 | COMP.RANK_RESULTS_GENDER_MISMATCH | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type whose Gender config value does not match the gender composition of their statistic participants, classifying each violation type, together with a coverage count of all eligible statistics. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `CONFIG_GENDER_TYPE_ID` | Tournament-owned statistics only; sport confirmed to populate the Gender config field. Distinguishes team-holding from athlete-holding statistics structurally, not by name | WRONG_GENDER |
| GLOBAL-DQ-045 | EVENT_DURATION_FULL_TIME_MISMATCH_TO_RANK | `RESULTS.sql` | Finds active finished events in the sport's timed disciplines containing at least one participant whose full-time duration result is missing despite an active Rank, present without an active Rank, or present with an invalid time format, together with the count and type of mismatching participants per event and a coverage count of all eligible events. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `TIMED_DISCIPLINE_LIST` | Sport with at least one timed discipline; judged disciplines must be left out of `TIMED_DISCIPLINE_LIST` or every judged participant is reported | WRONG_RESULTS |
| GLOBAL-DQ-046 | COMP.RANK_RESULTS_TIME_FULL_TIME_MISMATCH_TO_RANK | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type in the sport's timed disciplines containing at least one participant whose Time or Time Difference storage is inconsistent with their Rank, together with the distinct violation types and count of mismatching participants per statistic and a coverage count of all eligible statistics in those disciplines. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_RANK_TYPE_ID`, `DATA_TIME_TYPE_ID`, `DATA_TIME_DIFFERENCE_TYPE_ID`, `TIMED_DISCIPLINE_LIST` | Tournament-owned statistics only; the discipline list is read from the statistic layer (`object_discipline` owner type 83), which may cover a different population than the event layer | WRONG_RESULTS |
| GLOBAL-DQ-047 | EVENT_RESULTS_UNEXPECTED_FOR_NOT_STARTED | `RESULTS.sql` | Finds active events whose status is one of the sport's not-started detailed statuses that have at least one active, non-empty result row for any event participant, with status context, together with a coverage count of all eligible not-started events. | `SPORT_ID`, `NOT_STARTED_DESC_LIST` | Sport whose not-started detailed statuses all mean the event has not run; a status used for abandoned events must be left out of the list | WRONG_RESULTS |
| GLOBAL-DQ-048 | TOURNAMENT_STAGE_NAME_FORMAT_INVALID | `HIERARCHY.sql` | Finds each distinct active tournament-stage name that breaks at least one text-hygiene rule - edge or doubled spacing, a control character, a definite text corruption such as an HTML entity, a replacement character, a non-breaking or zero-width space or a double-encoded byte sequence, a non-ASCII character, a hyphen without surrounding spaces, a year glued to a word, a capitalisation shape a proof-read name does not take, a placeholder name or a numeric-only name - naming every rule the name breaks and how many objects carry it, reporting one row per offending name rather than one per object repeating it, together with a coverage count of all distinct eligible names. | `SPORT_ID` | Any sport with named active stages; `HYPHEN_WITHOUT_SPACES` reports a compound proper noun (`Saint-Denis`, `U-23`) and `NON_ASCII_CHARACTER` reports a legitimate diacritic, so both are named separately rather than merged into one verdict | MALFORMED_NAME |
| GLOBAL-DQ-049 | EVENT_NAME_FORMAT_INVALID | `HIERARCHY.sql` | Finds each distinct active event name that breaks at least one text-hygiene rule - edge or doubled spacing, a control character, a definite text corruption such as an HTML entity, a replacement character, a non-breaking or zero-width space or a double-encoded byte sequence, a non-ASCII character, a hyphen without surrounding spaces, a year glued to a word, a capitalisation shape a proof-read name does not take, a placeholder name or a numeric-only name - naming every rule the name breaks and how many objects carry it, reporting one row per offending name rather than one per object repeating it, together with a coverage count of all distinct eligible names. | `SPORT_ID` | Any sport with named active events; the same two noisy rules as `GLOBAL-DQ-048` apply, and a sport whose event names are codes rather than prose will report its whole vocabulary | MALFORMED_NAME |
| GLOBAL-DQ-050 | TOURNAMENT_STAGE_NAME_CASE_INCONSISTENT | `HIERARCHY.sql` | Finds each active tournament-stage name spelling that loses to a more common spelling of the same name within the sport - identical once case and spacing are ignored, different byte for byte - reporting only the losing spelling with the dominant one beside it rather than every stage in the disagreeing group, together with a coverage count of all distinct eligible stage name spellings. | `SPORT_ID` | Any sport with named active stages; where two spellings occur equally often no spelling can be called dominant, so both are reported under their own check_type and the reviewer chooses | MALFORMED_NAME |
| GLOBAL-DQ-051 | COMP.RANK_NAME_FORMAT_INVALID | `STATISTICS.sql` | Finds each distinct active tournament-owned statistic name that breaks at least one text-hygiene rule - edge or doubled spacing, a control character, a definite text corruption such as an HTML entity, a replacement character, a non-breaking or zero-width space or a double-encoded byte sequence, a non-ASCII character, a hyphen without surrounding spaces, a year glued to a word, a capitalisation shape a proof-read name does not take, a placeholder name or a numeric-only name - naming every rule the name breaks and how many objects carry it, reporting one row per offending name rather than one per object repeating it, together with a coverage count of all distinct eligible names. | `SPORT_ID`, `STATISTIC_TYPE_ID` | Tournament-owned statistics only; the same two noisy rules as `GLOBAL-DQ-048` apply | MALFORMED_NAME |
| GLOBAL-DQ-052 | EVENT_RESULTS_COMMENT_INVALID_OR_CONTRADICTED | `RESULTS.sql` | Finds active event-participant rows whose Comment result value is outside the sport's confirmed set of status codes, or whose Comment marks a participant as having no classified result while a Rank, a time or a Medal is stored for that same participant, together with a coverage count of all eligible event-participants carrying an active, non-empty Comment value. | `SPORT_ID`, `RESULT_COMMENT_TYPE_ID`, `RESULT_COMMENT_VALUE_LIST`, `RESULT_COMMENT_NO_RESULT_LIST`, `RESULT_RANK_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `RESULT_DURATION_TYPE_ID`, `RESULT_MEDAL_TYPE_ID` | Sport whose Comment result resolves to a closed set of status codes recorded in `RESULT_COMMENT_VALUE_LIST`; a sport storing free text there would report its whole population. `RESULT_COMMENT_NO_RESULT_LIST` holds every spelling that means the participant has no classified result, including one the value list rejects, so a contradiction is found regardless of how the status is spelled | WRONG_RESULTS |
| GLOBAL-DQ-053 | EVENT_RESULTS_MEDAL_RANK_MISMATCH | `RESULTS.sql` | Finds active event-participant rows carrying an active, non-empty Medal result whose Rank does not match the place the medal stands for, or that carry no Rank at all, together with a coverage count of all eligible event-participants carrying an active, non-empty Medal value. | `SPORT_ID`, `RESULT_MEDAL_TYPE_ID`, `RESULT_RANK_TYPE_ID` | Sport awarding gold, silver and bronze to places one, two and three; a tie is unaffected because each row is compared with its own Rank, but a sport mapping medals to other places needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-054 | EVENT_RESULTS_RANK_FULL_TIME_NOT_MONOTONIC | `RESULTS.sql` | Finds active events in the sport's timed disciplines holding a pair of participants whose Rank order contradicts their full-time duration order, so a better-ranked competitor is recorded as slower than a worse-ranked one, with the number of contradicting pairs per event, together with a coverage count of all eligible events holding at least two participants with both a numeric Rank and a readable full time. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `TIMED_DISCIPLINE_LIST` | Sport with at least one timed discipline whose finishing order follows the clock; a discipline ranked by judging or by points must be left out of `TIMED_DISCIPLINE_LIST` | WRONG_RESULTS |
| GLOBAL-DQ-055 | EVENT_PARTICIPANTS_DUPLICATE_IN_EVENT | `RESULTS.sql` | Finds active events in which one participant holds more than one active event-participant row, so the same competitor is entered twice in the same event, with the number of duplicated participants and their names, together with a coverage count of all eligible events holding at least one active participant. | `SPORT_ID` | Any sport where a competitor takes part once per event; a format entering the same participant twice by design needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-056 | EVENT_DURATION_FULL_TIME_ARITHMETIC_MISMATCH | `RESULTS.sql` | Finds active events in the sport's timed disciplines where a participant's full-time duration does not equal the leader's full time plus that participant's own gap value, beyond the tolerance the sport records, so the two time results disagree with each other rather than merely with their expected shape, with the number of disagreeing participants per event, together with a coverage count of all eligible events holding a readable leader full time and at least one participant carrying both a gap and a full time. | `SPORT_ID`, `RESULT_RANK_TYPE_ID`, `RESULT_DURATION_TYPE_ID`, `RESULT_FULL_TIME_TYPE_ID`, `TIMED_DISCIPLINE_LIST`, `FULL_TIME_TOLERANCE_SECONDS` | Sport confirmed to follow the leader/gap convention and to store an absolute full time beside it; `FULL_TIME_TOLERANCE_SECONDS` must match the precision of the source, and a value written in a shape the parser does not read is left out rather than reported | WRONG_RESULTS |
| GLOBAL-DQ-057 | COMP.RANK_RESULTS_COMMENT_INVALID_OR_CONTRADICTED | `STATISTICS.sql` | Finds active statistic-participant rows of the selected statistic type, excluding IOC-purpose templates, whose Comment data value is outside the sport's confirmed set of status codes, or whose Comment marks a participant as having no classified result while a Rank, a Time or a Medal is stored for that same participant, together with a coverage count of all eligible statistic-participant rows carrying an active, non-empty Comment value. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_COMMENT_TYPE_ID`, `DATA_COMMENT_VALUE_LIST`, `DATA_COMMENT_NO_RESULT_LIST`, `DATA_RANK_TYPE_ID`, `DATA_TIME_TYPE_ID`, `DATA_MEDAL_TYPE_ID` | Tournament-owned statistics only; the statistic layer is inventoried separately from the event layer, because the two are not guaranteed to share a vocabulary even within one sport | WRONG_RESULTS |

| GLOBAL-DQ-058 | EVENT_TEAM_PARTICIPANT_WITHOUT_LINEUP | `PARTICIPANTS.sql` | Finds active events holding at least one team event participant where the lineup layer is missing, separating an event in which no team resolves to any lineup member from one in which some teams do and others do not, with the number of teams on each side, together with a coverage count of all eligible events holding at least one active team event participant. | `SPORT_ID` | Sport that fields teams in events; a sport whose event participants are all individuals has no eligible population and its coverage count says so rather than the check reading clean | NO_RELATED_RECORDS |
| GLOBAL-DQ-059 | EVENT_RESULTS_DUPLICATE_ROWS | `RESULTS.sql` | Finds active events holding more than one active result row for the same event participant and result type, separating a duplicate repeating the same value from one storing conflicting values, with the number of affected participants and a sample group, together with a coverage count of all eligible events holding at least one active result. | `SPORT_ID` | Sport confirmed to store one active result row per participant and result type; a format that legitimately records the same result type twice for one participant needs its own statement | WRONG_RESULTS |
| GLOBAL-DQ-060 | COMP.RANK_RESULTS_DUPLICATE_ROWS | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type, excluding IOC-purpose templates, holding more than one active data row for the same statistic participant and data type, separating a duplicate repeating the same value from one storing conflicting values, with template and tournament name context, the number of affected participants and a sample group, together with a coverage count of all eligible statistics holding at least one active data row. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID` | Tournament-owned statistics only; shard confirmed per `DB-SEM-006`. The statistic layer is checked separately from the event layer because the two store their values in unrelated tables | WRONG_RESULTS |
| GLOBAL-DQ-061 | EVENT_STATUS_TIME_CONFLICT | `HIERARCHY.sql` | Finds active events whose status contradicts their own start date, being either a finished event dated in the future or a not-started event whose start date is older than the sport's staleness window, with template, stage and status context and the number of days elapsed, together with a coverage count of all eligible finished and not-started events. | `SPORT_ID`, `NOT_STARTED_DESC_LIST`, `STALE_NOT_STARTED_DAYS` | Sport whose events carry a start date and whose not-started detailed statuses all mean the event has not run. `STALE_NOT_STARTED_DAYS` is a judgement value rather than a structural fact, so a sport that publishes its calendar far ahead needs a wider window than one that does not | DATE_RANGE_MISMATCH |
| GLOBAL-DQ-062 | EVENT_DUPLICATE_BY_METADATA | `HIERARCHY.sql` | Finds groups of more than one active event sharing the same tournament stage, discipline set, exact start date, name and identical participant set, separating a group of empty event shells from one repeating the same competitors, so parallel heats that legitimately share a name and slot are excluded by their disjoint participant sets, together with a coverage count of all eligible named events. | `SPORT_ID` | Any sport with named, dated events. The participant set is part of the key precisely because a heats-based sport runs several identically named events in one slot; without it every such heat is reported | WRONG_RESULTS |
| GLOBAL-DQ-063 | TOURNAMENT_NO_STAGES | `HIERARCHY.sql` | Finds active tournaments, excluding IOC-purpose templates, with zero active tournament stages, so an individual empty tournament is reported even when its own template holds other populated tournaments and therefore never reaches GLOBAL-DQ-001, together with a coverage count of all eligible tournaments. | `SPORT_ID` | Any sport with active tournaments. Overlaps `GLOBAL-DQ-001` wherever a template is empty throughout; what it adds is the empty tournament inside an otherwise populated template, which the template-level check cannot see | NO_RELATED_RECORDS |
| GLOBAL-DQ-064 | COMP.RANK_ATHLETE_TEAM_MISSING_OR_INVALID | `STATISTICS.sql` | Finds active athlete statistic-participant rows of the selected statistic type, excluding IOC-purpose templates, inside a statistic that uses the Team data field, whose own Team value is absent, does not resolve to an active team participant, is stored more than once with conflicting values, or is stored more than once repeating one value, separating the four cases, with statistic, template and tournament name context, together with a coverage count of all eligible athlete statistic-participant rows inside statistics that use the Team data field. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_TEAM_TYPE_ID` | Tournament-owned statistics only; sport confirmed to store the athlete's team in the Team data field as a `participant.id`. The eligible population is the statistic that uses the field at all, so an individual-event statistic is excluded rather than reported as violating | MISSING_VALUES |
| GLOBAL-DQ-065 | COMP.RANK_TEAM_ATHLETE_COUNT_UNEVEN | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type, excluding IOC-purpose templates, whose athletes are assigned to teams through the Team data field in unequal numbers, so one team fields fewer athletes than another inside one statistic, separating a shortfall of one athlete from a larger one, with the team by team breakdown and template and tournament name context, together with a coverage count of all eligible statistics holding at least one athlete assigned to a resolvable team. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_TEAM_TYPE_ID` | Tournament-owned statistics only; sport whose teams field an identical number of athletes in one competition. A shortfall of one is separated from a larger one because a single missing athlete is often a real withdrawal, while a larger gap is almost always incomplete entry | WRONG_RESULTS |
| GLOBAL-DQ-066 | COMP.RANK_TEAM_GENDER_BALANCE_UNEVEN | `STATISTICS.sql` | Finds active tournament-owned statistics of the selected statistic type whose Gender config value is mixed, excluding IOC-purpose templates, holding at least one team whose athletes assigned through the Team data field are not an equal number of male and female, separating a team fielding one gender only from one fielding both in unequal numbers, with the offending teams and their gender counts and template and tournament name context, together with a coverage count of all eligible mixed statistics holding at least one athlete assigned to a resolvable team. | `SPORT_ID`, `STATISTIC_TYPE_ID`, `SHARD_ID`, `DATA_TEAM_TYPE_ID`, `CONFIG_GENDER_TYPE_ID` | Tournament-owned statistics only; sport whose mixed formats require a symmetric team composition. Measures each team on its own, which `GLOBAL-DQ-044` cannot do: a statistic holding both genders overall passes there while one of its teams fields men only | WRONG_GENDER |
| GLOBAL-DQ-067 | EVENT_MIXED_TEAM_LINEUP_GENDER_BALANCE_UNEVEN | `PARTICIPANTS.sql` | Finds active event-participant rows of mixed-gender teams whose lineup holds both male and female members in unequal numbers, with event, stage and participant context and the lineup gender counts, together with a coverage count of all eligible event-participants of mixed-gender teams whose lineup holds at least one member of each gender. | `SPORT_ID` | Sport whose mixed team formats require a symmetric lineup. A lineup missing one gender entirely is excluded so the finding is not a restatement of `GLOBAL-DQ-043`, which already reports it | WRONG_GENDER |

<!-- MANUAL PASTE ZONE: GLOBAL DQ REGISTRY — insert approved additions immediately before this marker; do not move or delete it. -->

## Qualification rule

A check qualifies as a GLOBAL DQ template only when:

- its relation path is supported by `DATABASE.md`;
- the violation it asserts is wrong in every applicable sport, not only in the sport it was
  first written for;
- every sport-varying input is a declared parameter;
- it contains no hard-coded sport ID or sport name;
- its applicability prerequisite is stated, so a sport where the condition is legitimately
  absent is excluded rather than reported as violating;
- one CheckID identifies exactly one executable statement.

The last point matters most in practice. "Every stage has an age class" is true for some
sports and meaningless for others; such a check is global only because the prerequisite
column says where it applies.

## Promoting a sport check to a template

Only after explicit user approval:

1. prove the violation condition is not sport-specific;
2. replace every sport literal with a declared parameter;
3. state the applicability prerequisite;
4. assign the next unused `GLOBAL-DQ-NNN`;
5. insert the statement in its domain file by CheckID order;
6. insert the registry row immediately before the marker above;
7. set the originating sport's registry `Family` to the new template ID;
8. run `TOOLS/Test-Package.ps1`.

A sport check keeps its own CheckID after promotion. `Family` records that the two are the
same logical check; the sport CheckID is never renumbered or reused.

## Manual registry-rule additions

Registry-policy changes are inserted immediately before the marker below.

<!-- MANUAL PASTE ZONE: GLOBAL DQ RULES — insert approved additions immediately before this marker; do not move or delete it. -->
