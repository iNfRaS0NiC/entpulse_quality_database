# Local credentials picked up by TOOLS/Run-Query.ps1 - never commit these.
*.local.ps1

# Query result exports.
output/

# The local record of which notifications have already been sent.
*.local.json
